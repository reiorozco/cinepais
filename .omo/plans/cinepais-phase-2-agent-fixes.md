# cinepais-phase-2-agent-fixes - Work Plan

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** El copiloto de la Fase C con todos los hallazgos del review corregidos: las dos pruebas que faltaban o apuntaban mal (agotado y sin-sillas-juntas) ahora prueban los escenarios reales, el bug que filtraba razonamiento en inglés queda bloqueado estructuralmente, las consultas por rango de fechas funcionan como se aprobó, un tool colgado ya no congela la conversación, y los límites anti-abuso quedan con tope real de memoria. Documentación y repositorio quedan sincronizados con la realidad.

**Why this approach:** Correcciones acotadas sobre el mismo branch — cero rediseño: cada fix ataca un hallazgo citado del review con su test que lo prueba, y los evals con LLM real se corren una sola vez al final para no gastar de más.

**What it will NOT do:** No toca la web ni despliega. No agrega herramientas ni cambia el modelo. No reescribe historia de git. Hay UN paso manual tuyo: rotar la API key de Gemini por precaución.

**Effort:** Medium
**Risk:** Low - fixes puntuales con evidencia archivo:línea; el único costo real es una corrida de evals (~centavos).
**Decisions to sanity-check:** Timeout de 45s para la herramienta de recomendación (medido, no adivinado); tope de sesiones con expiración de 1 hora (dos dependencias pequeñas nuevas: cachetools y pytest-timeout); guard "too_broad" que pide película o ciudad para búsquedas multi-día.

Your next move: correr `/start-work cinepais-phase-2-agent-fixes` en un chat fresco (o pedir primero el dual review de alta precisión). Full execution detail follows below.

---

> TL;DR (machine): Medium/Low — 7 fix-todos + F1-F4 on branch phase-2-agent: retarget soldout eval + new no-adjacent eval (SSE-payload asserts), thinking-part filter + namespaced tool match + empty_reply guard, date_range grammar + too_broad + parallel seat fetch, real per-tool timeouts (45/10s), TTLCache + sessionId≤128 + empty-message guard, docs sync, hygiene + key gate; deps +cachetools +pytest-timeout; no web/ edits, no push.

## Scope
### Must have
- All work on the EXISTING branch `phase-2-agent` (pre-merge fixes, no new branch). Fixes for every blocking finding of the 5-lane review (receipts in `.omo/drafts/cinepais-phase-2-agent-fixes.md`) + the 17 Metis directives (ses_025c2d173ffe…):
- Eval suite: soldout eval retargeted to the REAL planted scenario (film-02 "Sombras del Puente", Medellín) with structural SSE-payload assertions; NEW no-adjacent eval against the checkerboard scenario asserting `outcome=="degraded"`; token-usage table captured into evidence.
- `sse.py`: explicit Gemini thinking-part filter; namespaced tool-name guard; zero-token-turn semantics tested.
- `mcp_server.py`: full `date_range` grammar (window-bounded keywords, range ≤7d, `bad_date_range`), `too_broad` guard, zero-match hint, bounded-parallel seat fetches.
- `agent.py`: per-tool timeouts ACTUALLY applied (`recommend_best` 45s, others 10s) with a hung-child test.
- `main.py`: TTLCache-bounded session store, `sessionId` length validation, empty-message guard.
- Docs synced to reality: README event order + model chain, sse-contract event-ordering/latency/limits sections for Fase D.
- Hygiene: `agent/.gitignore`, cached `.pyc` purge, key-material grep gate + `[MANUAL]` rotation step.

### Must NOT have (guardrails, anti-slop, scope boundaries)
- NO web/ or specs/ edits. NO deploy artifacts. NO new MCP tools. NO model-chain rework in `llm.py` (code is the source of truth; docs sync TO it). NO Redis/external stores; NO caching layers beyond TTLCache. NO push, NO merge, NO attribution lines. NO new deps beyond `cachetools` (runtime) and `pytest-timeout` (dev), both with recorded justification + resolved versions in evidence. NO bare `endswith("recommend_best")` (false-positive matches). NO 20s timeout on `recommend_best` (measured latency exceeds it). NO unbounded date×seat fan-out (too_broad guard mandatory). NO weakening of existing green tests to make fixes pass.

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: tests-with-each-todo (project convention) — pytest + pytest-asyncio; unit tests network-free; evals (`-m evals`) against live web :3000 (seeded per the SEED_NOW=bare-`<tomorrow>` rule from the parent plan §Verification, `TZ=America/Bogota`) + real Gemini.
- Every todo runs: `cd agent && uv run ruff check . && uv run ruff format --check . && uv run basedpyright && uv run pytest <scope> -v`.
- The ONE key-material gate before any commit: `grep -rE "AIza|AQ\.[A-Za-z0-9_-]{8}" agent/ .omo/evidence/ --include="*.py" --include="*.md" --include="*.txt" --include="*.json" | grep -v ".env"` → zero matches.
- Evidence: .omo/evidence/task-<N>-cinepais-phase-2-agent-fixes.md (outside ulw-loop).

## Execution strategy
### Parallel execution waves
- Wave 1: todos 1, 2, 3, 4, 5 (parallel — disjoint files: sse.py · mcp_server.py · agent.py · main.py · hygiene)
- Wave 2: todo 6 (docs — documents the FINAL post-fix behavior incl. measured latency from todo 2's QA)
- Wave 3: todo 7 (eval suite re-run + new evals + token capture — after ALL code fixes so real-LLM spend happens once)
- Final wave: F1–F4

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 (sse) | — | 6, 7 | 2, 3, 4, 5 |
| 2 (mcp tools) | — | 6, 7 | 1, 3, 4, 5 |
| 3 (timeouts) | — | 6, 7 | 1, 2, 4, 5 |
| 4 (abuse bounds) | — | 6, 7 | 1, 2, 3, 5 |
| 5 (hygiene) | — | F1-F4 | 1, 2, 3, 4 |
| 6 (docs) | 1, 2, 3, 4 | 7 | — |
| 7 (evals) | 1, 2, 3, 4, 6 | 8 | — |
| 8 (recalibrated asserts) | 7 | 9 | — |
| 9 (evidence honesty) | 8 | 10 | — |
| 10 (product fix, Wave 5) | 9 | 11 | — |
| 11 (strict asserts, Wave 5) | 10 | 12 | — |
| 12 (integrity + fresh F-wave, Wave 5) | 10, 11 | F1-F4 | — |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. sse.py: thinking-part filter + namespaced tool-name guard + zero-token semantics
  What to do / Must NOT do: In `agent/src/cinepais_agent/sse.py`: (a) in the list-content normalization (currently :46-60), skip parts where `isinstance(p, dict) and p.get("type") == "thinking"` EXPLICITLY (Metis #4 — do not filter by "absence of text type"); keep the existing `if content:` guard so all-thinking chunks emit nothing. (b) Replace the exact tool-name check (:72) with a helper `_matches_recommend_best(name: str) -> bool` returning `name == "recommend_best" or name.endswith("__recommend_best")` (Metis #5 — MCP adapters may namespace as `cinepais__recommend_best`; bare `endswith("recommend_best")` is FORBIDDEN: matches `not_recommend_best`). (c) If an entire turn yields zero token events AND no recommendation event AND no error, emit `error` event `{code:"empty_reply", message:"No pude generar una respuesta. Intenta reformular tu pregunta."}` before `done` (guards the only-thinking-reply failure observed in BUG-1). Tests (tests/test_service.py or new tests/test_sse_stream.py): mixed thinking+text parts → only text streamed; ONLY-thinking stream → zero token events, `empty_reply` error, no crash; `_matches_recommend_best` positive ("recommend_best", "cinepais__recommend_best") and negative ("not_recommend_best", "recommend_best_v2" → note: endswith("__recommend_best") correctly rejects both); e2e stub streaming test where the stubbed astream_events emits a namespaced `on_tool_end` name and the `recommendation` event still fires. Must NOT: change the frozen SSE event contract shapes; touch other files.
  Parallelization: Wave 1 | Blocked by: — | Blocks: 6, 7
  References: agent/src/cinepais_agent/sse.py:46-76 (current normalization + name check); review lane2 BUG-1 transcript (.omo/evidence/review-c-qa-report.md — English "thought\n…" leak); lane3 MAJOR-3; Metis findings #4, #5 (ses_025c2d173ffe…); agent/docs/sse-contract.md (contract FROZEN — behavior only).
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/ -m "not evals" -v` all green incl. new named tests `test_thinking_parts_filtered`, `test_only_thinking_emits_empty_reply_error`, `test_tool_name_match_namespaced`, `test_recommendation_fires_with_namespaced_tool_name`; `uv run ruff check . && uv run basedpyright` exit 0.
  QA scenarios: happy = pytest stub stream with `[{"type":"thinking","text":"hmm"},{"type":"text","text":"Hola"}]` → single token "Hola"; failure = only-thinking stream → `empty_reply` error event captured, stream terminates with done. Evidence .omo/evidence/task-1-cinepais-phase-2-agent-fixes.md
  Commit: Y | fix(agent): filter Gemini thinking parts and harden recommend_best event matching

- [x] 2. mcp_server.py: date_range grammar + too_broad guard + zero-match hint + parallel seat fetch
  What to do / Must NOT do: In `agent/src/cinepais_agent/mcp_server.py`: (a) implement the plan-mandated `date_range` semantics for `recommend_best` (parent plan todo 6): accepted forms single `YYYY-MM-DD`, inclusive `YYYY-MM-DD..YYYY-MM-DD` (≤7 days), keywords `hoy|mañana|finde|semana` resolved against the DISCOVERABLE window (one un-dated `get_showtimes` fetch → [min,max] businessDate; `finde` = nearest Sat+Sun inside the window else latest weekend day(s) within it; `semana` = whole window; `hoy`/`mañana` only if inside window), `None` = `semana`; anything else → `{"error":"bad_date_range","hint":"YYYY-MM-DD, YYYY-MM-DD..YYYY-MM-DD, hoy|mañana|finde|semana"}`; expand to a date list and fan out one `get_showtimes(date=d, ...)` per date, union results. (b) TOO-BROAD GUARD (Metis #6 — DoS ceiling): when the expanded range has >1 date AND both `film_query` and `city` are None → `{"error":"too_broad","hint":"Especifica una película o ciudad para buscar en un rango de fechas"}` BEFORE any seat fetch. (c) Parallelize per-showtime seat fetches with `asyncio.gather` bounded by `asyncio.Semaphore(8)`; document worst bounded case (film+city+finde ≈ 6 showtimes → 1 batch) in a comment. (d) `search_showtimes`: when `_find_film` matches nothing anywhere, return `{"films_matched":[], "showtimes":[], "hint":"no matching films found"}` (lane3 MAJOR-4) instead of a bare list. Tests (respx-mocked): every grammar form; finde-on-Monday-window fixture (resolves to that window's Sat+Sun — parent plan test name `test_finde_resolves_within_seeded_window`); `bad_date_range`; `too_broad` (semana + no film/city → structured error, ZERO get_seats calls asserted via respx call count); zero-match hint; parallel fetch produces identical results to sequential (order-independence). Must NOT: unbounded gather; catching-all try/except that hides ValidationApiError (map it to a structured tool error instead).
  Parallelization: Wave 1 | Blocked by: — | Blocks: 6, 7
  References: agent/src/cinepais_agent/mcp_server.py:34-54 (_find_film), :81-91 (hint branch), :175-208 (recommend_best); lane3 MAJOR-1/MAJOR-4; Metis #6; parent plan .omo/plans/cinepais-phase-2-agent.md todo 6 (approved grammar, verbatim); f3-qa2.txt (sequential-fetch latency evidence); web/src/app/api/showtimes/route.ts:6-13 (date regex the API enforces).
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/test_mcp_server.py -v` all green incl. `test_finde_resolves_within_seeded_window`, `test_date_range_too_broad_no_seat_calls`, `test_bad_date_range`, `test_zero_match_hint`, `test_parallel_seat_fetch_deterministic`; ruff + basedpyright exit 0.
  QA scenarios: happy = `date_range="finde"` + film + city on a Monday-window fixture → union of Sat+Sun showtimes, seats fetched in parallel, deterministic Recommendation; failure = `date_range="semana"` with no film/city → `too_broad` error and respx records ZERO /seats calls. Evidence .omo/evidence/task-2-cinepais-phase-2-agent-fixes.md
  Commit: Y | fix(agent): implement date_range semantics with too_broad guard and parallel seat fetches

- [x] 3. agent.py: apply per-tool timeouts for real
  What to do / Must NOT do: In `agent/src/cinepais_agent/agent.py`: replace the dead `_MCP_TOOL_TIMEOUT = 20` with `_TOOL_TIMEOUTS = {"recommend_best": 45}` + `_DEFAULT_TOOL_TIMEOUT = 10` (Metis #7 — 20s would kill legitimate recommend_best calls; 45s covers the post-parallelization worst case measured in todo 2). APPLY them: after loading MCP tools, wrap each tool's async invocation (`tool.coroutine` / `ainvoke` — adapt to the installed langchain-core Tool surface) in `asyncio.wait_for(..., timeout=_TOOL_TIMEOUTS.get(base_name, _DEFAULT_TOOL_TIMEOUT))` where `base_name` strips any `<server>__` prefix; on `TimeoutError` raise a typed error that `sse.py`'s existing TimeoutError branch converts to the Spanish `error` event. Add dev dep `uv add --dev pytest-timeout` (Metis #10; record resolved version) so the hung-child test is itself bounded. Tests: stub tool that sleeps `timeout+1` → wrapped call raises TimeoutError within bounds (`@pytest.mark.timeout` parameterized FROM the constants, never hardcoded — Metis #7.2); base-name stripping test (`cinepais__recommend_best` → 45s bucket); fast tool unaffected. Must NOT: wrap the whole `astream_events` stream in one wait_for (kills legit long turns); leave the old constant or the docstring claim if unimplemented anywhere.
  Parallelization: Wave 1 | Blocked by: — | Blocks: 6, 7
  References: agent/src/cinepais_agent/agent.py:44-45 (dead constant), :58-87 (client + tools + checkpointer); agent/src/cinepais_agent/sse.py:78-85 (existing TimeoutError→error branch, currently unreachable); lane3 MAJOR-2; lane4 LOW-dead-timeout; Metis #7, #10.
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/test_agent_core.py -v` green incl. `test_tool_timeout_enforced`, `test_timeout_base_name_stripping`; `grep -n "wait_for" agent/src/cinepais_agent/agent.py` non-empty; `grep -rn "pytest-timeout" agent/pyproject.toml` non-empty; ruff + basedpyright exit 0.
  QA scenarios: happy = fast stub tool returns normally under both buckets; failure = sleeping stub tool → TimeoutError surfaced, and via the service stub test an `error` SSE event (not a hang), bounded by pytest-timeout. Evidence .omo/evidence/task-3-cinepais-phase-2-agent-fixes.md
  Commit: Y | fix(agent): enforce per-tool MCP timeouts (45s recommend_best, 10s default)

- [x] 4. main.py: TTLCache session store + sessionId validation + empty-message guard
  What to do / Must NOT do: In `agent/src/cinepais_agent/main.py`: (a) `uv add cachetools` (justification, Metis #9: TTL semantics required; stdlib OrderedDict has no TTL; zero transitive deps — record resolved version in evidence AND note it as an approved-new-dep for F4). Replace `_session_queries` defaultdict (:31) with `cachetools.TTLCache(maxsize=10_000, ttl=3600)` (counter semantics: `cache[sid] = cache.get(sid, 0) + 1`). (b) `ChatRequest.sessionId: Annotated[str, Field(min_length=1, max_length=128)]` (Metis #12 — also bounds MemorySaver thread_id keys). (c) Empty-message guard BEFORE the session-cap check (Metis #11): `if not body.message.strip():` → SSE `error` event `{code:"empty_message", message:"El mensaje no puede estar vacío."}` then done, ZERO LLM/agent invocation. Tests: TTL reset behavior with a short-ttl test fixture (cap reached → advance/expire → counter reset, cap allows again — proves eviction is intentional, Metis #8); 21-query cap still enforced within TTL (existing test adjusted, not weakened); `sessionId=""` → FastAPI 422, no LLM call; `sessionId` of 129 chars → 422; empty message → `empty_message` error event with zero agent calls (assert via stub injection). Must NOT: Redis; global state leak between tests (reset the cache in conftest as done for slowapi).
  Parallelization: Wave 1 | Blocked by: — | Blocks: 6, 7
  References: agent/src/cinepais_agent/main.py:31 (defaultdict), :88-90 (ChatRequest), :98-127 (chat handler order); lane4 HIGH-2 + LOW-sessionId; Metis #8, #9, #11, #12; agent/tests/test_abuse_controls.py + tests/conftest.py:6-21 (slowapi reset pattern to mirror).
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/test_abuse_controls.py tests/test_service.py -v` green incl. `test_session_cap_ttl_reset`, `test_session_id_length_422`, `test_empty_message_error_event_no_llm_call`; `grep -n "TTLCache" agent/src/cinepais_agent/main.py` non-empty; ruff + basedpyright exit 0.
  QA scenarios: happy = 20 queries pass, 21st gets Spanish `session_cap_exceeded`, after TTL expiry the same sessionId works again; failure = `{"message":"","sessionId":"x"}` → `empty_message` error event and the stubbed agent records ZERO invocations. Evidence .omo/evidence/task-4-cinepais-phase-2-agent-fixes.md
  Commit: Y | fix(agent): bound session store with TTLCache, validate sessionId, guard empty messages

- [x] 5. Repo hygiene: .gitignore + cached .pyc purge + key-material gate
  What to do / Must NOT do: (a) Create `agent/.gitignore` with: `__pycache__/`, `*.pyc`, `.env`, `.venv/`, `.pytest_cache/`, `dist/`. (b) `git rm -r --cached` every tracked `__pycache__`/`*.pyc` under agent/ (lane1 WARN: 30 files); verify `git status --porcelain | grep -c pyc` → 0 and `git ls-files 'agent/**/__pycache__/*'` → empty. (c) Key-material gate (Metis #17): run `grep -rE "AIza|AQ\.[A-Za-z0-9_-]{8}" agent/ .omo/evidence/ --include="*.py" --include="*.md" --include="*.txt" --include="*.json"` → MUST be zero matches (`.env` is untracked and excluded); if matches found, redact the file and re-run. (d) `[MANUAL — USER]` step documented in the final report and README security section (todo 6 wires the README text): rotate GOOGLE_API_KEY in Google AI Studio as a precaution (a prefix appeared in review-lane logs); the plan cannot execute this. Must NOT: touch `.env` itself; rewrite git history (no rebase — the .pyc removal is a normal commit).
  Parallelization: Wave 1 | Blocked by: — | Blocks: F1-F4
  References: lane1 finding (30 committed .pyc, no agent/.gitignore); lane4 HIGH-1 (key hygiene + rotation rationale); Metis #16, #17 (exact grep + [MANUAL] marking).
  Acceptance criteria (agent-executable): `test -f agent/.gitignore`; `git ls-files 'agent/**/*.pyc' | wc -l` prints 0; the key-material grep exits with zero matches; working tree otherwise clean.
  QA scenarios: happy = fresh `uv run pytest` regenerates __pycache__ and `git status` stays clean (ignore works); failure = planting a fake `AIzaFAKE1234` string in a scratch evidence file makes the gate fail loudly (then remove it). Evidence .omo/evidence/task-5-cinepais-phase-2-agent-fixes.md
  Commit: Y | chore(agent): add .gitignore, purge cached bytecode, add key-material gate

- [x] 6. Docs sync: README + sse-contract reflect post-fix reality
  What to do / Must NOT do: (a) `agent/README.md`: fix the SSE example to the REAL order for tool turns (`tool_call → recommendation → token* → done`, Metis #13); sync the "Model fallback chain" section VERBATIM to `llm.py:23-28` and fix the architecture line to "tries `langchain.agents.create_agent` (langchain 1.x), falls back to `langgraph.prebuilt.create_react_agent`" (Metis #14); document session-cap TTL semantics ("resets after 1 hour or restart — courtesy limit"); add the `[MANUAL]` key-rotation step to the security section; keep `--timeout=120` now that pytest-timeout is installed. (b) `agent/docs/sse-contract.md`: add an "Event ordering" section (tool turns vs non-tool turns, Fase D must handle BOTH); add "token events may be absent on tool-only turns — render the recommendation card without preceding text" (Metis #4); add a latency note with the POST-FIX measured range from todo 2's QA (expected ~5-45s for recommend_best depending on breadth — replace with the measured number); document `sessionId` 1-128 chars in the request schema and the `empty_message`/`empty_reply`/`too_broad`/`bad_date_range` error codes. Must NOT: change event SHAPES (contract frozen); document aspirations — every number/order stated must come from a todo's evidence.
  Parallelization: Wave 2 | Blocked by: 1, 2, 3, 4 | Blocks: 7
  References: agent/README.md:104-117 (wrong order), :166 (stale chain), :87 (timeout flag), :127 (cap framing); agent/docs/sse-contract.md (ordering + limits gaps); agent/src/cinepais_agent/llm.py:23-28 (source of truth); Metis #13, #14, #4.2, #8.2, #12; todo 2 evidence (measured latency).
  Acceptance criteria (agent-executable): `grep -n "tool_call" agent/README.md` appears BEFORE the first `token` line in the SSE example (verify with `grep -n` line numbers); `grep -A5 "fallback chain" agent/README.md` matches the llm.py list exactly; `grep -n "Event ordering" agent/docs/sse-contract.md` non-empty; `grep -n "empty_message\|too_broad\|bad_date_range" agent/docs/sse-contract.md` non-empty; `grep -n "128" agent/docs/sse-contract.md` non-empty.
  QA scenarios: happy = every README command block executed verbatim from a clean shell succeeds; failure = a doc claim without matching evidence (cross-check the latency number against task-2 evidence) fails this todo's self-review. Evidence .omo/evidence/task-6-cinepais-phase-2-agent-fixes.md
  Commit: Y | docs(agent): sync README and SSE contract to post-fix behavior

- [x] 7. Eval suite: fix soldout, add no-adjacent, capture token usage — full re-run
  What to do / Must NOT do: Prereq (executor-owned, ONCE): web on :3000, re-seed `SEED=20260801 SEED_NOW=<tomorrow-bare-YYYY-MM-DD> pnpm prisma db seed`, `TZ=America/Bogota`, GOOGLE_API_KEY present. In `agent/tests/evals/test_queries.py`: (a) RETARGET `test_sold_out_with_alternative` (Metis #1): query `"Quiero ver Sombras del Puente en Medellín, ¿hay sillas disponibles?"`; PRE-CHECK via API (Metis #2): `GET /api/showtimes?filmId=film-02&city=Medellín` must return the site-med-1 imax day-0 showtime (else `pytest.skip("soldout showtime filtered by cutoff — re-seed")`) and its seats endpoint must report `availableCount == 0` (proves planted state); capture the SSE stream through the service layer (httpx ASGI streaming against the app with the REAL agent) and assert `recommendation.outcome == "no_availability"` AND `len(alternatives) > 0` AND Spanish narration without discouraging phrases. (b) ADD `test_no_adjacent_alternative` (Metis #3): dynamically discover the checkerboard showtime — `GET /api/showtimes?city=Bogotá&date=<day3>` → the site-bog-1 2D showtime (film is PRNG-random, NEVER hardcode it); ask for 2 juntas for that film/date/city; assert `recommendation.outcome == "degraded"` AND `len(seatIds) == 1` AND `len(alternatives) > 0` AND trajectory called an adjacency/seat tool. (c) Token usage (Metis #15): sum `AIMessage.usage_metadata` across eval runs (fallback: document why unavailable) and write the table `model | input_tokens | output_tokens | est_cost_USD` into this todo's evidence; fix the README cost pointer if the location changes. (d) Re-run the FULL eval + security suite. Must NOT: weaken any existing assertion to pass; hardcode the checkerboard film id or absolute dates; exceed ~60 LLM calls total.
  Parallelization: Wave 3 | Blocked by: 1, 2, 3, 4, 6 | Blocks: F1-F4
  References: web/prisma/seed.ts:79 (film-02 title), :240-263 (soldout plant), :295-298 (checkerboard cols), :256-276 (no forced film at site-bog-1); agent/tests/evals/test_queries.py:252-265 (current wrong test); agent/tests/evals/conftest.py:35-112 (fixtures to reuse); Metis #1, #2, #3, #15; lane5 BLOCK-1/2; parent plan success criteria #2.
  Acceptance criteria (agent-executable): `cd agent && TZ=America/Bogota uv run pytest tests/evals -m evals -v --timeout=120` → ALL green (12 existing incl. re-targeted soldout + 1 new no-adjacent + 3 security = 13+); the two named tests `test_sold_out_with_alternative` and `test_no_adjacent_alternative` pass with their structural payload asserts; evidence contains the token-usage table and full transcripts.
  QA scenarios: happy = full eval run green with transcripts + usage table saved; failure = stop the web server → conftest exits with the friendly infra message (existing behavior, re-verified); soldout pre-check failing → skip with the documented message, never a false pass. Evidence .omo/evidence/task-7-cinepais-phase-2-agent-fixes.md
  Commit: Y | test(agent): fix soldout eval, add no-adjacent eval, record token usage

- [x] 8. Recalibrated structural eval assertions (closes review-round-2 BLOCK-A — tests/evidence only, NO product-code changes)
  What to do / Must NOT do: CONTEXT: round-2 review proved the agent's behavior is CORRECT (broad soldout query → `recommended` with available Premium/2D seats, soldout showtime surfaced among alternatives = the killer feature) and the original `no_availability` expectation was miscalibrated for broad queries; the executor then silently dropped the required asserts. This todo adds the asserts back, RECALIBRATED. In `agent/tests/evals/test_queries.py`: (a) PAYLOAD CAPTURE helper: extract the `recommend_best` tool result from the trajectory (the ToolMessage whose tool name is `recommend_best` or endswith `__recommend_best`), parse via `Recommendation`/`RecommendationEvent.model_validate_json` (tool returns model_dump_json — mcp_server.py) — no SSE harness needed. (b) SOLDOUT DUAL-QUERY design: `test_sold_out_narrow_no_availability` — query pins the ONLY candidate: "Quiero 2 sillas para Sombras del Puente en IMAX en CinePaís El Poblado el <day0-date>" (film-02 + Medellín + IMAX + date computed from the API pre-check, NEVER hardcoded) → assert payload `outcome == "no_availability"` AND `len(alternatives) > 0` (branch-3 contract from the parent plan todo 5: alternatives MUST carry real options outside the queried criteria — if this fails it is a REAL bug, surface it, do not weaken); keep the existing API pre-check (showtime present, availableCount==0). `test_sold_out_broad_never_discourage` (retarget of the existing test) — broad query "¿hay sillas para Sombras del Puente en Medellín?" → assert payload `outcome == "recommended"` AND main `showtimeId != soldout_id` AND `soldout_id ∈ [a.showtimeId for a in alternatives]` (the tradeoff is SURFACED, spec decisión #3) AND existing Spanish/no-discourage checks. (c) NO-ADJACENT conditional-strict: reword query to bias the tool: "Recomiéndame 2 sillas juntas para <film> el <day3-date> en Bogotá en 2D" (film discovered dynamically as today); THEN: IF `recommend_best` in trajectory → assert payload `outcome == "degraded"` AND `len(seatIds) == 1` AND `len(alternatives) > 0`; ELSE `adjacent_seats` MUST be in trajectory → parse ITS tool result → assert `groups == []` AND the reply offers an alternative (existing no-discourage check + reply mentions another función/horario — regex `otra|alternativa|horario|función`); either branch is strict, neither is skippable. Must NOT: modify scoring.py/mcp_server.py/sse.py or any product behavior; weaken ANY other assertion; hardcode dates/film ids/showtime ids.
  Parallelization: Wave 4 | Blocked by: 7 (builds on its retarget + pre-checks) | Blocks: F1-F4
  References: round-2 review receipts (draft §Delta re-review round 2); lane2 BLOCKING-1 fix-option 1 (narrow query wording); lane5 BLOCK-A (grep proof of absent asserts + ToolMessage extraction fix); f3-fixes-qa1.txt (observed broad-query payload shape: soldout listed in alternatives); parent plan todo 5 branch contract (alternatives outside criteria); web/prisma/seed.ts:240-263 (soldout = film-02/site-med-1/imax/day0; site-med-1 = "CinePaís El Poblado"); agent/tests/evals/conftest.py (date fixtures).
  Acceptance criteria (agent-executable): `cd agent && TZ=America/Bogota uv run pytest tests/evals -m evals -v --timeout=180` ALL green including the three (re)named tests; `grep -c "outcome" agent/tests/evals/test_queries.py` ≥ 4 (asserts present, not comments); full unit suite still green.
  QA scenarios: happy = narrow query transcript shows outcome no_availability + alternatives with Spanish reasons; failure = if branch-3 alternatives come back empty on the narrow query, the test FAILS LOUDLY (real bug surfaced, report it — do not adjust the assert). Evidence .omo/evidence/task-8-cinepais-phase-2-agent-fixes.md (full transcripts + parsed payloads)
  Commit: Y | test(agent): recalibrated structural payload assertions for soldout and no-adjacent evals

- [x] 9. Evidence honesty + doc pointer repairs (closes lane-5 IMPORTANT findings)
  What to do / Must NOT do: (a) Regenerate F3 evidence HONESTLY per the recalibrated criteria: re-run the two curl SSE scenarios (narrow soldout → expect `no_availability` in the raw stream; broad soldout → expect `recommended` + soldout id among alternatives), save raw streams to .omo/evidence/f3-fixes-r2-qa{1,2}.txt, and REWRITE f3-fixes-summary.md with criteria-as-written verdicts — the previous summary declared PASS by reframing a criterion miss; the new summary must state each criterion verbatim, the observed result, and PASS/FAIL against it with zero reinterpretation. (b) Fix `agent/README.md:150` cost pointer → `.omo/evidence/task-8-cinepais-phase-2-agent-fixes.md` (where the current eval transcripts + usage estimates now live; keep the "estimated — usage_metadata unavailable" caveat verbatim). (c) Complete `.omo/evidence/task-7-cinepais-phase-2-agent-fixes.md`: replace "Pending commit" with the real hash (`git log --format='%h %s' | grep 'fix soldout eval'` → e22ff00 or current) and append a pointer to task-8 for the recalibrated run. Must NOT: alter raw stream files after capture; touch product code; re-litigate closed findings.
  Parallelization: Wave 4 | Blocked by: 8 (documents its outcomes) | Blocks: F1-F4
  References: lane5 discovered_context items (f3 summary reframing, README:150 stale pointer, task-7 "Pending commit"); .omo/evidence/f3-fixes-summary.md (the file to rewrite); todo 8 evidence.
  Acceptance criteria (agent-executable): `grep -n "task-8-cinepais-phase-2-agent-fixes" agent/README.md` non-empty and `grep -n "task-9-cinepais-phase-2-agent.md" agent/README.md` empty; `grep -n "Pending commit" .omo/evidence/task-7-cinepais-phase-2-agent-fixes.md` empty; f3-fixes-summary.md contains each criterion quoted + observed + verdict (grep for "Criterion" ≥ 2); raw f3-fixes-r2-qa*.txt files exist and contain `event:` lines.
  QA scenarios: happy = summary verdicts match the raw streams a reader can check themselves; failure = any criterion-vs-observed mismatch labeled PASS → this todo fails its own self-review. Evidence .omo/evidence/task-9-cinepais-phase-2-agent-fixes.md
  Commit: Y | docs(agent): honest F3 evidence regeneration and stale pointer repairs

- [x] 10. PRODUCT FIX (user-authorized, Option A): alternatives contract in scoring + soldout tradeoff surfacing
  What to do / Must NOT do: The ONLY product-code todo — user explicitly authorized on 2026-08-07 after round-3 exposed the gaps. (a) `agent/src/cinepais_agent/events.py`: `Alternative.qualityTier: Literal["low","optimal","high"] | None` (a soldout entry has no ratable seats; "low" would lie) — additive, null-safe. (b) `agent/src/cinepais_agent/scoring.py` (+helpers; keep ≤250 LOC): implement the branch-3 ALTERNATIVES WIDENING contract — when the computed outcome is `no_availability` or `degraded` AND alternatives from the original candidate pool number <3, run DETERMINISTIC widening passes in FIXED order over the existing fan-out machinery: (w1) drop `format` filter; (w2) drop `city` filter; (w3) extend dates to the full discoverable window; STOP as soon as ≥1 alternative is found; cap 3 total; dedupe by showtimeId; reasons per step: "otro formato disponible" / "disponible en otra ciudad" / "otra fecha disponible". Bounded: ≤3 extra `get_showtimes` passes, seat fetches inside the existing `Semaphore(8)`. (c) SOLDOUT TRADEOFF: when outcome=="recommended" AND the queried candidate pool contains ≥1 showtime with availableCount==0, append ONE alternative for the EARLIEST such soldout showtime (deterministic tie-break: businessDate, time, id) with `qualityTier=None`, `reason="esta función está agotada"` — even if that makes 4 entries (cap becomes 3+1 tradeoff entry). (d) `agent/docs/sse-contract.md`: document nullable qualityTier + the tradeoff entry semantics ("Fase D renders soldout alternatives disabled/informational"). (e) `agent/src/cinepais_agent/prompts.py`: replace the three natural-phrase sentinels with synthetic canaries (e.g. distinct `PROMPT-CANARY-<hex>` strings embedded as never-output markers) — fixes the structurally flaky `test_injection_resilience` (SENTINEL_1 was a phrase the agent legitimately says). Unit tests: widening order + stop + cap + dedupe (fixtures per step); soldout tradeoff entry appended deterministically; nullable qualityTier round-trip; canary absence in a normal reply fixture. Must NOT: touch any other product file; change event names/shapes beyond the nullable field; alter the model chain or timeouts; exceed 250 LOC/module.
  Parallelization: Wave 5 | Blocked by: — | Blocks: 11, 12
  References: round-3 lane2 raw streams review-c3-qa-narrow.txt (`alternatives:[]` on no_availability) + review-c3-qa-broad.txt (soldout absent from alternatives); draft §Round-4 design analysis (widening order, nullability rationale, canary rationale); parent plan todo 5 branch-3 contract (the spec this implements); scoring.py/scoring_helpers.py current structure; events.py:21-48; prompts.py:5-7 sentinels.
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/ -m "not evals" -v 2>&1 | tee /tmp/t10-unit.txt` all green incl. new `test_widening_order_and_cap`, `test_soldout_tradeoff_entry`, `test_alternative_nullable_quality`, `test_canaries_not_in_normal_reply`; ruff + basedpyright exit 0; `git diff --name-only HEAD~1` limited to the files named above.
  QA scenarios: happy = fixture with zero-availability sole candidate → outcome no_availability with 1-3 widened alternatives each carrying the correct step reason; failure = widening finds nothing anywhere (fully-soldout synthetic world) → alternatives==[] allowed ONLY then, reasoning explains honestly. Evidence .omo/evidence/task-10-cinepais-phase-2-agent-fixes.md (raw tee outputs only)
  Commit: Y | feat(agent): alternatives widening contract, soldout tradeoff surfacing, synthetic prompt canaries

- [x] 11. Strict asserts, correctly calibrated (no conditionals, no skippable branches, no unreachable demands)
  What to do / Must NOT do: In `agent/tests/evals/test_queries.py` — now PASSABLE because todo 10 exists; the STOP-rule applies: if any assert fails, STOP and report the failing raw payload in evidence — do NOT modify the assert, do NOT add conditionals. (a) `test_sold_out_narrow_no_availability`: UNCONDITIONAL `assert payload.outcome == "no_availability"` AND `assert len(payload.alternatives) > 0` (remove the `if payload.outcome == "recommended":` block entirely). (b) `test_sold_out_broad_never_discourage`: keep outcome=="recommended" + showtimeId != soldout_id, ADD `assert soldout_id in [a.showtimeId for a in payload.alternatives]` (todo 10c guarantees it) AND `assert next(a for a in payload.alternatives if a.showtimeId == soldout_id).qualityTier is None`. (c) `test_no_adjacent_alternative` — RECALIBRATED per the reachability discovery (draft §Round-4): the `degraded` outcome is structurally unreachable E2E for the checkerboard (tool params cannot pin a site; other Bogotá sites always offer adjacent pairs) — it is already covered by scoring-unit + service-stub tests. The eval now asserts the DETERMINISTIC parts directly: (i) in-process direct call `adjacent_seats(checkerboard_id, 2)` (import the MCP tool fn or call through the server session) → `assert result["groups"] == []` and the honest note present; (ii) agent query (any natural wording) → reply Spanish + non-discouraging + regex `otra|alternativa|horario|función` + trajectory called ≥1 seat tool. NO showtimeId gate, NO skippable branch — every assert runs every time. Add a comment block in the test citing the reachability rationale so future reviewers do not re-add the E2E degraded demand. (d) Delete the task-8 evidence "Calibration note" superseded by this todo (replace file content with the new raw run). Must NOT: touch other tests; weaken anything; leave any conditional assert path.
  Parallelization: Wave 5 | Blocked by: 10 | Blocks: 12
  References: round-3 lane1 blocking items 1-3 (the exact missing asserts); draft §Round-4 design analysis (reachability); todo 10 (the behaviors these asserts now verify); test_queries.py:319-451 current weakened forms.
  Acceptance criteria (agent-executable): `cd agent && TZ=America/Bogota uv run pytest tests/evals -m evals -v --timeout=180 2>&1 | tee /tmp/t11-evals.txt` → ALL green (incl. the previously-flaky injection test, now canary-based); `grep -n "if payload.outcome" agent/tests/evals/test_queries.py` → empty; `grep -c "assert" agent/tests/evals/test_queries.py` recorded in evidence from the raw tee.
  QA scenarios: happy = full eval suite green with the three strict tests' raw transcripts saved; failure = any strict assert fails → executor STOPS, saves the failing payload verbatim to evidence, reports — zero test modifications. Evidence .omo/evidence/task-11-cinepais-phase-2-agent-fixes.md (raw tee outputs only)
  Commit: Y | test(agent): strict calibrated payload assertions for soldout and no-adjacent evals

- [x] 12. Evidence integrity restoration + fresh F-wave
  What to do / Must NOT do: ANTI-SLOP CONTRACT for this todo (violations = automatic FAIL in review): every number in evidence MUST come from a `cmd 2>&1 | tee <evidence-file>` raw capture; NO derived/summarized counts; NO checkbox may be marked without naming its evidence file. (a) Fix the fabricated verification in `.omo/evidence/task-9-cinepais-phase-2-agent-fixes.md`: re-run `grep -c "event:" .omo/evidence/f3-fixes-r2-qa1.txt` live via tee (real value: 18) and replace the false "6"; add a one-line erratum noting the correction (do not silently rewrite history). (b) Rewrite `.omo/evidence/f3-fixes-summary.md` verdicts as criteria-as-written PASS/FAIL ONLY (the "MISCALIBRATED" category is abolished; interpretive rationale allowed solely in a separate "Notes" section) — and since todo 10 changed behavior, RE-RUN the three F3 curls fresh (narrow soldout → expect no_availability + non-empty alternatives now; broad → recommended + soldout entry with null qualityTier; finde → completes) saving raw streams to f3-fixes-r3-qa{1,2,3}.txt, and verdict against the criteria as now written in this plan. (c) FRESH F-wave: re-execute F1-F4 per their definitions and write a NEW evidence file `.omo/evidence/f-wave-r2-cinepais-phase-2-agent-fixes.md` (never edit the old one); only then re-check the F boxes, each line citing its evidence path. F2 addendum: the todo-10 diff gets a focused self-review pass (it is the only product change since the round-2 code/security PASS — list the diff and confirm scope). Must NOT: edit raw stream files post-capture; mark any box without evidence; reuse stale F-wave content.
  Parallelization: Wave 5 | Blocked by: 10, 11 | Blocks: —
  References: round-3 lane5 BLOCKING-2 (fabricated grep, real value 18) + IMPORTANT findings (stale F-wave re-check via 88adc11, MISCALIBRATED category); f3 criteria as defined in this plan's F3 row (updated by todo 10 behavior); anti-slop contract above.
  Acceptance criteria (agent-executable): live `grep -c "event:" .omo/evidence/f3-fixes-r2-qa1.txt` output matches what task-9 evidence now states; `grep -n "MISCALIBRATED" .omo/evidence/f3-fixes-summary.md` → empty; `test -f .omo/evidence/f-wave-r2-cinepais-phase-2-agent-fixes.md`; f3-fixes-r3-qa1.txt contains `"outcome":"no_availability"` AND a non-empty `"alternatives":[` (grep both); every re-checked F box cites an evidence path.
  QA scenarios: happy = a reader can re-run every quoted command and get the quoted output byte-for-byte; failure = any quoted number diverges from its raw file → this todo fails its own self-review. Evidence .omo/evidence/task-12-cinepais-phase-2-agent-fixes.md
   Commit: Y | docs(agent): evidence integrity restoration and fresh F-wave

<!-- ═══ ROUND 5 (2026-08-07): after the round-4 delta review FAILED 0/4. User decisions: OPTION A (recalibrate narrow E2E to invariants; enum lives at unit/tool level) + CAMINO B (2 tight todos, model-priority swap included, drop cosmetic cleanup, keep 4-lane review for rigor). GROUND RULE (hard): `.omo/plans/**` is PLANNER-ONLY — any executor edit to a plan file is an automatic review FAIL (round-4 commit 72f5688 rewrote F3 mid-round; that class of act is now a contract violation). Reconciliations binding on reviewers — DO NOT re-litigate: (1) `mcp_widening.py` placement is BLESSED (todo 10's file list was planner error; widening needs the async client; scoring.py is I/O-free by design). (2) The stop-at-≥1 early-break is plan-as-written, NOT a bug — the doc drift is the real defect, fixed inline in todo 14. (3) The narrow-query outcome enum is NON-DETERMINISTIC at E2E (LLM widens autonomously before recommend_best; worker's narrow2.txt caught no_availability once, review re-runs got recommended). Never re-demand the enum E2E. See draft §ROUND-4 RESULTS + §Camino B rationale. -->

- [x] 13. Widening bug fixes + Lite-first model priority + real tests (min viable set)
  What to do / Must NOT do: THREE surgical changes, no scope creep. (a) `agent/src/cinepais_agent/mcp_widening.py`: (a1) soldout classifier — guard the vacuous truth: `c.seats and all(s.status != "Available" for s in c.seats)`; empty seat list is a data gap, NOT soldout (must never surface as "esta función está agotada"). (a2) qualityTier fallback — a widened candidate with zero seats gets `qualityTier=None`, never `"low"` (silent lie). (a3) TRADEOFF DEDUPE: before appending the soldout tradeoff entry, REMOVE any existing alternatives entry with the same showtimeId (the honest `null`/"agotada" version wins) — kills the round-4 live duplicate. (a4) Add ONE comment on the stop-at-≥1 break citing draft §ROUND-4 RESULTS reconciliation so future reviewers don't re-flag. SKIP `# type: ignore` cleanup (cosmetic, not blocking — draft §Camino B). (b) `agent/src/cinepais_agent/llm.py:23-28`: reorder `_MODEL_PRIORITY` to prefer Lite variants first: `["gemini-3.5-flash-lite", "gemini-2.5-flash-lite", "gemini-3.5-flash", "gemini-1.5-flash"]`. Rewrite the comment at :20-22: cost-first rationale — Lite is materially cheaper on output at parity for our workload (tool-calling + short Spanish narration); prefer it and let the invoke fallback catch availability gaps. (c) `agent/tests/test_scoring_widening.py`: REPLACE the three placebo tests with THREE real tests driving `apply_widening` through a stubbed client/fetch_fn: (t1) empty-seats candidate NOT classified soldout (asserts the tradeoff entry is NOT appended for that id); (t2) tradeoff entry via ACTUAL `apply_widening` — earliest soldout picked deterministically (order by businessDate, time, id), `qualityTier is None`, reason contains "agotada", REPLACES any same-id pre-existing entry; (t3) widening dedupe — a widened result whose showtimeId overlaps an original-pool entry is deduped (single occurrence in the returned alternatives). SKIP w1→w2→w3 chaining coverage (existing shape proven by round-4 widening-path QA; adding it now is scope creep). Must NOT: change widening step order/reasons/cap; touch prompts.py, agent.py, mcp_server.py; add new modules; edit `.omo/plans/**`.
  Parallelization: Wave 6 | Blocked by: — | Blocks: 14
  References: draft §ROUND-4 RESULTS lanes 1-3 (placebo tests, classifier, dupe) + draft §Camino B rationale (cost/scope decisions); mcp_widening.py current 119 LOC; review-c4-qa-narrow.txt (live duplicate evidence); llm.py:20-28 (priority chain to invert).
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/ -m "not evals" -v 2>&1 | tee /tmp/t13-unit.txt` all green; the three named test behaviors above appear as test names in test_scoring_widening.py (grep each); `uv run ruff check . && uv run basedpyright` exit 0; `grep -n "gemini-3.5-flash-lite" agent/src/cinepais_agent/llm.py | head -1` returns a line NUMBERED BEFORE any line matching `gemini-3.5-flash"` (Lite comes first); `git diff --name-only HEAD~1` ⊆ {agent/src/cinepais_agent/mcp_widening.py, agent/src/cinepais_agent/llm.py, agent/tests/test_scoring_widening.py}.
  QA scenarios: happy = stub fixtures exercise classifier + dedupe + tradeoff deterministically; agent startup log line reports chosen model as a `-lite` variant (screenshot/tee the startup log to evidence). failure = any placebo pattern remains OR chosen model is a non-Lite variant → FAIL. Evidence .omo/evidence/task-13-cinepais-phase-2-agent-fixes.md (raw tee only)
  Commit: Y | fix(agent): widening classifier + tradeoff dedupe + Lite-first model priority

- [x] 14. OPTION A test recalibration + single Alternative schema + evidence erratum
  What to do / Must NOT do: THREE changes, doc + test + evidence. (a) `agent/tests/evals/test_queries.py`: rename `test_sold_out_narrow_no_availability` → `test_sold_out_narrow_invariants`; assert ONLY the deterministic invariants (every assert runs every time, ZERO skippable branches): (1) `payload.outcome in {"recommended", "no_availability"}`; (2) `entries = [a for a in payload.alternatives if a.showtimeId == soldout_id]; assert len(entries) == 1` (presence AND no duplicate — catches the round-4 dupe live); (3) `entries[0].qualityTier is None` and `"agotada" in entries[0].reason`; (4) `actionable = ([payload.showtimeId] if payload.outcome == "recommended" else []) + [a.showtimeId for a in payload.alternatives if a.showtimeId != soldout_id]; assert len(actionable) >= 1`; (5) Spanish reply + non-discouragement regex + no English thinking (keep existing forms). Extraction expressions/ternaries are fine; `if`-STATEMENTS gating asserts are not. Replace the test's rationale comment block: cite draft §ROUND-4 RESULTS + user decision Option A (LLM autonomous widening ⇒ enum non-deterministic E2E; enum is covered at unit level by todo-13 tests + tool level by the widening-path QA). STOP-rule: if any recalibrated assert fails, STOP, save the raw payload to evidence, report — do NOT adjust. (b) `agent/docs/sse-contract.md`: DELETE the stale non-nullable Alternative schema block (~lines 116-128) — exactly ONE schema remains (nullable qualityTier); rewrite the widening prose to match implementation: "hasta 3 intentos secuenciales de relajación (formato → ciudad → fechas); se detiene en el primer intento que produce ≥1 alternativa; un solo intento puede producir hasta 3"; document the tradeoff-wins dedupe rule (same showtimeId → soldout tradeoff replaces any other entry). (c) Append an addendum to `.omo/evidence/task-12-cinepais-phase-2-agent-fixes.md`: erratum documenting (i) the qa1-vs-narrow2 filename mismatch against the then-current criterion, (ii) the unauthorized plan edit 72f5688 (planner-only rule now in force), (iii) pointer to the FINAL planner-authored F3 criteria below; note `f3-fixes-r3-narrow2.txt` remains as genuine evidence of E2E nondeterminism. SKIP the f-wave-r3 UNAUTHORIZED header prepend (cosmetic — the addendum in task-12 already documents it). Must NOT: touch `test_sold_out_broad_never_discourage`, `test_no_adjacent_alternative`, or any other test; weaken assertions; edit `.omo/plans/**`; touch product code beyond sse-contract.md; delete or modify any raw *.txt capture.
  Parallelization: Wave 6 | Blocked by: 13 | Blocks: F1-F4
  References: user decisions Option A + Camino B (2026-08-07); draft §ROUND-4 RESULTS reconciliation + §Camino B rationale; review-c4-evals.txt (failing enum assert + LLM autonomous-widening trajectory); test_queries.py current strict forms from todo 11; sse-contract.md:112-146 (dup schema block).
  Acceptance criteria (agent-executable): `cd agent && TZ=America/Bogota uv run pytest tests/evals -m evals -v --timeout=180 2>&1 | tee /tmp/t14-evals-run1.txt` ALL green, then SECOND consecutive full run teed to /tmp/t14-evals-run2.txt also ALL green (nondeterminism derisk — two runs, not "rerun-until-green"); `grep -n "test_sold_out_narrow_no_availability" agent/tests/evals/test_queries.py` empty; `grep -En "^\s*if .*payload" agent/tests/evals/test_queries.py` empty; `grep -c "\*\*Schema:\*\*" agent/docs/sse-contract.md` recorded from tee (target: exactly the count for the pre-existing NON-Alternative schemas + 1 for the surviving Alternative schema — no duplicate); `grep -n "72f5688" .omo/evidence/task-12-cinepais-phase-2-agent-fixes.md` non-empty.
  QA scenarios: happy = both consecutive eval runs green with raw transcripts saved; Fase-D reader of sse-contract.md sees exactly one Alternative schema with accurate widening description. failure = runs disagree (one green one red) → report as flakiness finding with BOTH raw payloads, do NOT rerun-until-green (nondeterminism is real signal per §ROUND-4). Evidence .omo/evidence/task-14-cinepais-phase-2-agent-fixes.md (raw tee only)
   Commit: Y | test(agent): Option-A narrow invariants + single Alternative schema + evidence erratum

<!-- ═══ ROUND 6 (2026-08-07): after round-5 delta review FAILED 0/4, reducing to TWO issues (see draft §ROUND-5 RESULTS). GROUND-RULE REFINEMENT (supersedes the round-5 rule, which was too broad and self-contradictory with the OMO completion workflow): the executor MAY flip checkbox state (`- [ ]`→`- [x]`) and commit `.omo/boulder.json` / `.omo/drafts/**` / `.omo/notepads/**` as normal completion bookkeeping. The executor MUST NOT edit any PROSE inside `.omo/plans/**` — not criteria, not "What to do", not acceptance text, not QA scenarios (that is the 72f5688 class). RATIFICATION: round-5 commit d081979's plan hunk is planner-authored content (todos 13/14 + FINAL F3 criteria I wrote uncommitted; the executor only swept them in + flipped boxes). Both L1 and L5 verified NO criteria were rewritten. It is HEREBY RATIFIED — do NOT revert it. USER DECISION (model): FIX FLASH AS DOCUMENTED DEFAULT (the Lite swap in todo 13b was a real regression — gemini-3.5-flash-lite never calls recommend_best → breaks the SSE contract, 2/2 reproducible; gemini-2.5-flash-lite is 404-dead). All round-5 green evidence actually ran on gemini-3.5-flash. Everything else in round 5 is VERIFIED CLEAN — todo 15 touches ONLY the model config, nothing else re-opens. -->

- [x] 15. Revert the Lite regression: pin gemini-3.5-flash as the documented default + re-validate E2E
  What to do / Must NOT do: The ONLY round-6 change; scope is model configuration + docs, NOTHING else. (a) `agent/src/cinepais_agent/llm.py`: reorder `_MODEL_PRIORITY` back to Flash-first — `["gemini-3.5-flash", "gemini-1.5-flash"]` as the safe head; REMOVE `gemini-2.5-flash-lite` entirely (repo evidence task-9-cinepais-phase-2-agent.md:56 records it returns 404 despite appearing in models.list()); you MAY keep `gemini-3.5-flash-lite` only at the TAIL as a last-resort (after the known-good models), never ahead of `gemini-3.5-flash`. RESTORE the deleted deprecation comment (the `# gemini-2.x / gemini-2.5-flash deprecated for new API keys` knowledge). DELETE the false comment claiming "the invoke fallback catches availability gaps" (it does not — `_discover_model_via_list()` short-circuits invoke). DELETE or rewrite the unsubstantiated "Lite performs at parity" claim (no eval backs it; state it as a cost tradeoff if kept). (b) Make shipped config = validated config: set `AGENT_MODEL_OVERRIDE=gemini-3.5-flash` in `agent/.env.example` with a comment explaining it pins the validated model (Lite broke tool-calling; see draft §ROUND-5 RESULTS), and note it in `agent/README.md` model-chain section (align the doc that F1 previously flagged for drift). (c) Cost note for the reader (README): Flash is ~5× Lite on output but chosen for reliable function-calling; low-traffic portfolio demo likely within Gemini free tier; the real cost lever (deterministic intent-parser, Camino C) is parked for a future phase. Must NOT: touch mcp_widening.py, scoring.py, events.py, prompts.py, agent.py, mcp_server.py, sse.py, any test file, or any evidence raw capture; edit PROSE in `.omo/plans/**`; re-open widening work; re-run the full eval suite twice (budget — see acceptance).
  Parallelization: Wave 7 | Blocked by: — | Blocks: F1-F4
  References: draft §ROUND-5 RESULTS Issue B (L2 no-recommend_best on Lite, L3 404 model + short-circuit); user model decision 2026-08-07 (Flash documented default); llm.py:20-28 current Lite-first chain; task-9-cinepais-phase-2-agent.md:56 (404 evidence); agent/README.md model-chain section.
  Acceptance criteria (agent-executable): `grep -n "gemini-3.5-flash\"" agent/src/cinepais_agent/llm.py | head -1` line NUMBERED BEFORE any `gemini-3.5-flash-lite` line (Flash-first); `grep -c "gemini-2.5-flash-lite" agent/src/cinepais_agent/llm.py` → 0 (removed); `grep -n "invoke fallback" agent/src/cinepais_agent/llm.py` → empty (false claim gone); `grep -n "AGENT_MODEL_OVERRIDE=gemini-3.5-flash" agent/.env.example` non-empty; `cd agent && uv run pytest tests/ -m "not evals" -v 2>&1 | tee /tmp/t15-unit.txt` all green (unchanged suite); `uv run ruff check . && uv run basedpyright` exit 0; `git diff --name-only HEAD~1` ⊆ {agent/src/cinepais_agent/llm.py, agent/.env.example, agent/README.md}.
  QA scenarios (BUDGET: exactly ONE E2E curl, ~$0.01 — this is the decisive proof the round-5 gate lacked): with web seeded + agent on :8000 running the ACTUAL SHIPPED DEFAULT (start the agent and confirm the startup/first-request log reports `gemini-3.5-flash`, NOT a -lite variant — tee the log), run ONE narrow soldout curl "Quiero 2 sillas para Sombras del Puente en IMAX en CinePaís El Poblado el <day0>" → the raw stream MUST contain a `recommendation` event (proves recommend_best IS called on the shipped model — the exact thing Lite failed) with the soldout id once, `qualityTier:null`, "agotada", ≥1 actionable, Spanish. failure = no recommendation event on the shipped default → STOP and report (do not swap models again without user). Evidence .omo/evidence/task-15-cinepais-phase-2-agent-fixes.md (raw tee: llm.py diff + startup log showing gemini-3.5-flash + the curl stream).
   Commit: Y | fix(agent): pin gemini-3.5-flash as documented default, drop dead/unreliable Lite chain

<!-- ═══ ROUND 7 (2026-08-13): after round-6, F3 REJECT exposed a real product gap (F3-R6-001) + planner's own contradictory test. USER DECISIONS: (A) PRODUCT FIX — surface the soldout "agotada" tradeoff on BOTH the `recommended` AND `no_availability` branches (deterministic; fixes the latent eval flake). This AUTHORIZES reopening mcp_widening.py (round-5-verified) — same explicit authorization todo 10 required. (B) MODEL — pin `gemini-3.6-flash` as the documented default WITH a fresh 1-curl E2E proof it calls recommend_best + emits a recommendation event (benchmarks are NOT trusted — Lite taught us that; if it fails, revert to gemini-3.5-flash and report, do not thrash). PLANNER ERROR OWNED: todo-14's `test_sold_out_narrow_invariants` accepts outcome ∈ {recommended, no_availability} then unconditionally demands the soldout entry — unsatisfiable on the no_availability branch pre-fix. The product fix (16) makes it satisfiable on BOTH branches, so the eval assertion needs NO change; the deterministic lock is a new UNIT test in todo 16. Also bundling the zero-LLM observability 1-liner (F3-R6-002) so the shipped model IS logged. Round-6 reconciliations remain binding. -->

- [x] 16. PRODUCT FIX (user-authorized): surface soldout tradeoff on BOTH outcome branches + deterministic unit lock
  What to do / Must NOT do: (a) `agent/src/cinepais_agent/mcp_widening.py`: the soldout tradeoff (`_build_soldout_tradeoff` → `qualityTier=None` + "esta función está agotada") is currently gated at `:67` on `outcome == "recommended"`. RESTRUCTURE so the tradeoff is built + deduped + appended on `recommended` AND `no_availability` (and `degraded`) — i.e. whenever the candidate pool contains a soldout showtime that is not the primary `result.showtimeId`. ORDER: run the availability widening FIRST (so `no_availability` still gets ≥1 actionable non-soldout alternative), THEN append the soldout tradeoff (deduped by showtimeId — the null/"agotada" version wins over any widened entry for the same id). The existing `_build_soldout_tradeoff` guards stay: skip when no soldout exists or when the soldout IS the primary. Keep widening step order/reasons/cap unchanged; the tradeoff is the one extra entry (cap becomes ≤3 widened + 1 tradeoff, as sse-contract already documents for the recommended path — now also true for no_availability). (b) `agent/tests/test_scoring_widening.py`: ADD a deterministic unit test `test_soldout_tradeoff_on_no_availability_branch` that builds a `RecommendationEvent(outcome="no_availability", showtimeId=None, alternatives=[...widened...])` with a soldout candidate in the pool, calls the REAL `apply_widening`, and asserts the soldout id appears in `result.alternatives` EXACTLY ONCE with `qualityTier is None` and "agotada" in reason, AND ≥1 non-soldout actionable alternative remains. This is the deterministic guarantee the E2E eval cannot provide. (c) `agent/docs/sse-contract.md`: update the tradeoff prose — the soldout "agotada" entry now surfaces on both `recommended` and `no_availability` outcomes (Fase D renders it disabled/informational either way). Must NOT: change widening reasons/order/cap; touch llm.py, model config, prompts.py, agent.py, sse.py; edit `.omo/plans/**` prose; weaken any existing test. STOP-rule: if the new unit test can't be made green without altering widening semantics, STOP and report.
  Parallelization: Wave 8 | Blocked by: — | Blocks: 17, F1-F4
  References: draft §ROUND-6 RESULTS F3-R6-001 (root cause, both-branch requirement) + user decision A (2026-08-13); mcp_widening.py:67 (the gate), :27-56 (_build_soldout_tradeoff), :77-130 (widening block); f3-fixes-r5-qa2.txt (the no_availability capture with 0 soldout entries); events.py RecommendationEvent shape.
  Acceptance criteria (agent-executable): `cd agent && uv run pytest tests/ -m "not evals" -v 2>&1 | tee /tmp/t16-unit.txt` all green incl. `test_soldout_tradeoff_on_no_availability_branch` (grep the name); the existing 3 round-5 widening tests still green (no regression); `uv run ruff check . && uv run basedpyright` exit 0; `git diff --name-only HEAD~1` ⊆ {agent/src/cinepais_agent/mcp_widening.py, agent/tests/test_scoring_widening.py, agent/docs/sse-contract.md}.
  QA scenarios: happy = unit fixture proves the agotada entry appears on the no_availability branch deterministically + widened actionable alternatives coexist; failure = tradeoff crowds out all actionable alternatives (actionable count 0) OR appears twice → FAIL. Evidence .omo/evidence/task-16-cinepais-phase-2-agent-fixes.md (raw tee only)
  Commit: Y | fix(agent): surface soldout tradeoff on no_availability branch + deterministic unit lock

- [x] 17. Model → gemini-3.6-flash (documented default) + model-logging observability + E2E proof
  What to do / Must NOT do: (a) `agent/src/cinepais_agent/llm.py`: put `gemini-3.6-flash` at the HEAD of `_MODEL_PRIORITY` (before `gemini-3.5-flash`, which stays as the proven fallback); keep `gemini-1.5-flash` tail; do NOT reintroduce `gemini-2.5-flash-lite` (404-dead). (b) `agent/.env` AND `agent/.env.example`: set `AGENT_MODEL_OVERRIDE=gemini-3.6-flash` (shipped = validated) with a comment; note it in `agent/README.md` model-chain section. (c) OBSERVABILITY (F3-R6-002, zero-LLM): add `logging.basicConfig(level=logging.INFO)` (or a uvicorn log-config) in `agent/src/cinepais_agent/main.py` startup so `llm.py`'s "Using model" INFO record actually reaches the server log — the agent must be able to report its own model in production logs (Fly.io deploy value). Must NOT: touch mcp_widening.py or any widening/scoring/test-widening file (that's todo 16); edit `.omo/plans/**` prose; re-run the full eval suite. STOP-rule: if the E2E proof shows gemini-3.6-flash does NOT call recommend_best / emits no recommendation event (the Lite failure mode), STOP, revert `_MODEL_PRIORITY`+override to `gemini-3.5-flash`, save the failing stream to evidence, and report — do NOT try other models without the user.
  Parallelization: Wave 8 | Blocked by: 16 | Blocks: F1-F4
  References: user decision B (2026-08-13, pin 3.6-flash with fresh E2E); draft §ROUND-6 RESULTS (Lite failure mode = no recommend_best call; benchmarks not trusted); F3-R6-002 (observability 1-liner); llm.py _MODEL_PRIORITY; main.py startup.
  Acceptance criteria (agent-executable): `grep -n "gemini-3.6-flash" agent/src/cinepais_agent/llm.py | head -1` line NUMBERED BEFORE any `gemini-3.5-flash` line; `grep -n "AGENT_MODEL_OVERRIDE=gemini-3.6-flash" agent/.env.example` non-empty; `grep -n "basicConfig\|log_config\|dictConfig" agent/src/cinepais_agent/main.py` non-empty; `cd agent && uv run pytest tests/ -m "not evals" -v 2>&1 | tee /tmp/t17-unit.txt` all green; `uv run ruff check . && uv run basedpyright` exit 0; `git diff --name-only HEAD~1` ⊆ {agent/src/cinepais_agent/llm.py, agent/src/cinepais_agent/main.py, agent/.env.example, agent/README.md}.
  QA scenarios (BUDGET: exactly ONE E2E curl, ~$0.01): with web seeded + agent on :8000, confirm the SERVER STARTUP LOG now reports `gemini-3.6-flash` (the observability fix must make this visible — tee it), then ONE narrow soldout curl "Quiero 2 sillas para Sombras del Puente en IMAX en CinePaís El Poblado el <day0>" → raw stream MUST contain a `recommendation` event with the soldout id once, `qualityTier:null`, "agotada" (now guaranteed on both branches by todo 16), ≥1 actionable, Spanish, no dup. failure = no recommendation event → execute the STOP-rule revert. Evidence .omo/evidence/task-17-cinepais-phase-2-agent-fixes.md (raw tee: llm.py diff + startup log showing gemini-3.6-flash + the curl stream).
   Commit: Y | feat(agent): pin gemini-3.6-flash default + model-logging observability

<!-- ═══ ROUND 8 (2026-08-13): Fase C is already CLOSED (rounds 1-7, 4/4 APPROVE). This is a single parked-cleanup item the user authorized before Fase D, NOT a re-opening of the gate. gemini-3.6-flash uses fixed sampling defaults and IGNORES `temperature` → langchain emits a UserWarning on every call and the setting is dead config (surfaced by F3 round-7 caveat #2). Trivial fix; verified by planner inline; no F-wave needed (cosmetic, no behavior change — the model already ignored the value). -->

- [x] 18. Cleanup: drop dead `temperature` config that gemini-3.6-flash ignores (kills per-call UserWarning)
  What to do / Must NOT do: `agent/src/cinepais_agent/llm.py` — REMOVE the `temperature=0.3` kwarg from the real model construction in `get_llm()` (line 128, inside the final `init_chat_model(...)`), since gemini-3.6-flash uses fixed sampling defaults and silently discards it (langchain logs `UserWarning: ... temperature will be ignored` every call). ALSO remove `temperature=0.0` from the discovery probe in `_discover_model_via_invoke()` (line 80) for consistency — that probe only pings availability with `max_tokens=10`; the value is equally dead there. Keep `max_tokens`/`max_output_tokens` (still honored). Must NOT: touch `_MODEL_PRIORITY`, the override logic, or any other file; change model behavior (this is a pure no-op removal — the model already ignored the value). This is planner-executed cleanup, not an executor round.
  Parallelization: standalone | Blocked by: — | Blocks: —
  References: f3-wave-r6-cinepais-phase-2-agent-fixes.md §Caveats #2 (the UserWarning + dead-config finding); llm.py:80, llm.py:128.
  Acceptance criteria: `grep -c "temperature" agent/src/cinepais_agent/llm.py` → 0; `cd agent && uv run pytest tests/ -m "not evals" -q` all green; `uv run ruff check . && uv run basedpyright` exit 0; `git diff --name-only HEAD~1` == `agent/src/cinepais_agent/llm.py` only.
  QA scenarios: happy = unit suite green + no temperature refs remain; failure = any behavior test regresses (there should be none — the value was already ignored). Evidence: inline planner verification (lsp diagnostics + grep), no separate evidence file needed for a no-op cleanup.
  Commit: Y | chore(agent): drop dead temperature config ignored by gemini-3.6-flash

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
<!-- F-wave INVALIDATED across rounds 2-6. Round-7 re-runs ALL F-lanes fresh after todos 16-17; write .omo/evidence/f{1..4}-wave-r6-cinepais-phase-2-agent-fixes.md (per-lane, as prior rounds did); every box cites its evidence path. BINDING reconciliations (do NOT re-flag): d081979 plan hunk RATIFIED; refined rule permits checkbox/boulder/drafts bookkeeping; mcp_widening.py placement blessed; stop-at-≥1 break is plan-as-written. NEW round-7 binding fact: the soldout "agotada" tradeoff is now REQUIRED on BOTH the recommended AND no_availability branches (todo 16) — F3 NARROW is now deterministically satisfiable regardless of the LLM's enum trajectory; the shipped model is gemini-3.6-flash (todo 17), proven E2E to call recommend_best. -->
- [x] F1. Plan compliance audit
  Every todo's acceptance command (1-17) re-runs green from the current checkout; the two new deps (cachetools, pytest-timeout) have recorded versions + justifications; evidence files task-1..17 exist. Reviewer lanes: NO branch switching, NO concurrent re-seeding (Fase B lesson). Round-5/6/7 reconciliations in their comment blocks are binding — do not re-flag blessed/ratified items.
- [x] F2. Code quality review
  `cd agent && uv run ruff check . && uv run ruff format --check . && uv run basedpyright` exit 0; no module >250 LOC; no dead constants; no weakened tests; no placebo test patterns in test_scoring_widening.py. Round-7 delta: mcp_widening builds the soldout tradeoff on both `recommended` and `no_availability` branches (deduped, widening order/reasons/cap unchanged); `_MODEL_PRIORITY` head is `gemini-3.6-flash`; main.py configures root logging so the model line reaches the server log.
- [x] F3. Real manual QA (FINAL criteria) + SHIPPED-MODEL PROOF (now 3.6-flash + both-branch soldout)
  With web seeded + agent on :8000 (single lane owns servers/DB). FIRST confirm the SERVER STARTUP LOG reports `gemini-3.6-flash` (the todo-17 observability fix must make this literally visible — no more "log reports nothing"; tee the log). Then: (1) BROAD curl "Sombras del Puente en Medellín" → `outcome:"recommended"` + soldout IMAX El Poblado id in alternatives EXACTLY ONCE with `qualityTier:null` + `reason` containing "agotada". (2) NARROW curl "Sombras del Puente en IMAX en CinePaís El Poblado el <day0>" → MUST emit a `recommendation` event; INVARIANTS (now deterministic on BOTH enum branches per todo 16): outcome ∈ {recommended, no_availability}; soldout id in alternatives exactly once with `qualityTier:null` + "agotada"; ≥1 actionable non-soldout option; Spanish; no discouragement; NO duplicate per showtimeId. (3) `date_range` query ("la mejor función del finde") → completes without timeout, latency logged. Capture raw streams to .omo/evidence/f3-fixes-r6-qa{1,2,3}.txt. Verdicts criteria-as-written PASS/FAIL only. NOTE: scenarios 1 & 3 MAY be cited from prior rounds only if todo 17 leaves their behavior unchanged AND the model is re-proven; the NARROW capture MUST be fresh on gemini-3.6-flash.
- [x] F4. Scope fidelity
  `git diff phase-1-ui...phase-2-agent --name-only | grep -v '^agent/' | grep -v '^.omo/'` empty; still exactly 4 MCP tools; no deploy files; new deps limited to cachetools + pytest-timeout; `git ls-files 'agent/**/*.pyc'` empty; key-material grep zero matches; commits conventional, no attribution, nothing pushed. Round-7 hygiene: confirm round-7 commits touched NO plan PROSE (checkbox-only flips permitted); the model override change is intentional and documented.

## Commit strategy

One commit per todo (7 total, messages given per-todo), conventional style, scope `agent`, on `phase-2-agent` only. NO push, NO merge, NO history rewrite, NO attribution lines. Working tree clean after each wave.

## Success criteria

1. All 7 todo acceptance commands green + F1–F4 APPROVE.
2. [SUPERSEDED 2026-08-07 by user decision Option A — see Round-5 block] The soldout scenarios close via deterministic invariants: narrow query surfaces the planted soldout exactly once (`qualityTier:null` + "agotada") with ≥1 actionable option and zero discouragement (spec §122's intent); the `no_availability`/widening enum contract is proven at unit level (todo 13 stub tests) and tool level (widening-path QA); no-adjacent closes per the todo-11 recalibration (spec §123 intent, reachability rationale in draft).
3. Full suite green: unit (`-m "not evals"`) AND evals (13+ incl. security) with zero weakened assertions; thinking-leak class of failure structurally prevented (filter + empty_reply guard + tests).
4. Abuse surface bounded: TTLCache session store, sessionId ≤128, empty-message guard, per-tool timeouts real, too_broad guard — each with a passing test.
5. Docs match reality: README event order/model chain, sse-contract ordering + latency + limits sections sourced from measured evidence; token-usage table recorded.
6. Hygiene: no tracked bytecode, `agent/.gitignore` present, key-material grep clean; `[MANUAL]` key-rotation step surfaced to the user in the final report.
