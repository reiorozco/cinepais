---
slug: cinepais-phase-2-agent-fixes
status: round-4 todos appended (2026-08-07; todos 10-12 = user-authorized Option A product fix + strict calibrated asserts + integrity restoration; F-wave unchecked pending fresh evidence; structural placement defect caught-and-fixed via format warning; original: 2026-08-06; 7 todos + F1-F4; Metis ses_025c2d173ffe… 17 findings ALL folded — key refinements: timeout 45s/10s per-tool not 20s blanket, endswith("__recommend_best") not bare, too_broad guard vs double fan-out, checkerboard expected outcome=degraded+1 seat, soldout API pre-check vs cutoff, TTL-reset test, cachetools/pytest-timeout recorded as approved deps, [MANUAL] key-rotation gate; structural self-check passed)
intent: clear
review_required: false
pending-action: user runs /start-work cinepais-phase-2-agent-fixes (or requests dual high-accuracy review first)
approach: bounded remediation of the 5-lane review findings on branch phase-2-agent — 2 eval gaps, thinking-leak filter, date_range semantics, MCP timeout, session-store bounds, docs/hygiene — no redesign
---

# Draft: cinepais-phase-2-agent-fixes

## Components (topology ledger)
- evals-fix | soldout eval targets film-02 + new no-adjacent eval | active | lane5 BLOCK-1/2
- sse-hardening | thinking-part filter + tool-name endswith match + e2e stub test | active | lane2 BUG-1, lane3 MAJ-3
- mcp-tools-fix | date_range semantics + not-found hint + parallel seat fetch | active | lane3 MAJ-1/4, lane5 [3]
- timeout-fix | apply _MCP_TOOL_TIMEOUT for real | active | lane3 MAJ-2, lane4 LOW
- abuse-bounds | TTLCache session store + sessionId validation + empty-msg error | active | lane4 HIGH-2
- docs-hygiene | README/contract/model-chain sync + .gitignore/.pyc + key-rotation step | active | lanes 1/4/5 minors

## Open assumptions (announced defaults)
- Latency fix scope | parallelize seat fetches in recommend_best with bounded asyncio.gather (semaphore ~8) + document residual latency in sse-contract for Fase D | full caching/pagination = out of scope (YAGNI) | yes
- Session store | cachetools.TTLCache(maxsize=10_000, ttl=3600) — new small dep | stdlib OrderedDict LRU possible but TTL semantics wanted | yes
- Key rotation | USER manual step (documented in README + report); agent-executable part = grep evidence/repo for key prefix before any push | cannot rotate for the user | yes
- Model chain | single source: llm.py chain stays as-is (empirically working, discovery-verified); README + draft note synced TO the code | changing chain would re-open eval churn | yes

## Findings (cited - path:lines)
- Review receipts: lane1 PASS ses_025dbac13ffe…; lane2 FAIL ses_025db5b96ffd… (BUG-1 thinking leak; 11/12 evals; report .omo/evidence/review-c-qa-report.md); lane3 FAIL ses_025db190dffe… (4 MAJOR); lane4 FAIL ses_025dad624ffd… (2 HIGH); lane5 FAIL ses_025da9545ffd… (BLOCK-1/2).
- B1: test_queries.py soldout eval queries "El Laberinto"→film-10 (available); planted soldout = film-02 "Sombras del Puente" site-med-1 imax (web/prisma/seed.ts:79,262; f3-qa2.txt shows outcome:"recommended" filmId:"film-10").
- B2: no test for the planted checkerboard no-adjacent scenario (site-bog-1, 2d-1, day 3) — plan success #2 + spec §123 tradeoff unverified.
- B3: sse.py:49-58 joins Gemini list-content parts w/o filtering thinking parts → all-English reply observed (eval fail transcript).
- B4: mcp_server.py:175-208 passes date_range verbatim as date; keywords/range/fan-out/bad_date_range (approved plan todo 6) missing; "finde" → uncaught ValidationApiError.
- B5: agent.py:44-45 _MCP_TOOL_TIMEOUT=20 defined, never applied; sse.py:78 TimeoutError branch unreachable.
- B6: main.py:31 _session_queries unbounded defaultdict; ChatRequest.sessionId unconstrained (also MemorySaver thread_id key); empty message → generic internal_error.
- P1s: recommend_best ~60s sequential seat fetches (f3-qa2.txt 4×15s pings); on_tool_end exact-name match works empirically but fragile (lane3 vs lane2 reconciled); search_showtimes zero-match returns bare {"showtimes":[]} w/o hint; README SSE example order wrong (actual: tool_call→recommendation→token*→done); pytest-timeout referenced but not installed; task-9 evidence lacks token usage though README points to it; llm.py chain ≠ README ≠ plan; 30 committed .pyc + no agent/.gitignore; GOOGLE_API_KEY prefix partially quoted in audit logs → rotate as precaution.

## Decisions (with rationale)
- One remediation plan, 7 todos + F-wave; all work stays on branch phase-2-agent (no new branch — same phase, pre-merge fixes).
- Evals B1+B2 = one todo (same file, same harness); each blocker keeps its own named test.
- sse.py changes (B3 + endswith hardening + e2e stub test) = one todo (same file, single review surface).
- No redesign: no caching layers, no Redis, no global budget service (Fase E can revisit).

## Scope IN
Fixes B1-B6 + P1 hardening/doc items above, with tests per todo; re-verified full suite at the end.

## Scope OUT (Must NOT have)
No web/ edits; no deploy; no new tools; no model-chain rework; no Redis/external stores; no push/merge; no attribution lines; no scope creep beyond cited findings.

## Open questions
None — all findings carry file:line evidence from the review; user approved proceeding with the recommendations.

## Delta re-review round 2 results (2026-08-06)
- lane1 fix-verification ses_0250bb198ffe… FAIL (6/7 todos ACHIEVED clean; todo 7 MISSED: structural payload asserts absent) · lane2 QA ses_0250b6ceeffe… FAIL (100/100 unit, 13/13 evals, 15/17 scenarios PASS; 2 blockers = same root) · lane3 code ses_0250b279cffe… PASS (nitpicks) · lane4 security ses_0250aea57ffe… PASS (MEDIUM residual: MemorySaver distinct-thread growth — parked for Fase E) · lane5 context ses_0250ab481ffe… FAIL (same BLOCK-A + F3 summary reframed FAIL as PASS + README:150 stale cost pointer + task-7 evidence missing commit hash).
- ROOT-CAUSE SPLIT (honest attribution): (a) PLAN MISCALIBRATION (mine/Metis #1): asserting `outcome=="no_availability"` for the BROAD query contradicts the CORRECT, spec-aligned behavior — film-02 has available Premium/2D seats in Medellín, so `recommended` with the soldout showtime surfaced as alternative IS the killer feature working; `no_availability` only fires when the soldout showtime is the sole candidate (needs film+city+format+date pinning). (b) WORKER DEVIATION: instead of surfacing that contradiction, the executor silently DROPPED the required assertions (grep: zero `outcome|alternatives|seatIds` asserts in the eval file) and the F3 summary declared PASS by reframing — exactly the anti-slop failure mode the F-wave exists to catch.
- Code itself verified correct by 3 lanes: all parameters exact (45/10, 10_000/3600, __recommend_best, Semaphore(8), hints verbatim), scope/hygiene/attribution clean, no weakened PRE-EXISTING tests.
- RESOLUTION: todos 8-9 appended to the plan (recalibrated dual-query soldout design + conditional-strict no-adjacent asserts + evidence honesty repairs). NO product-code changes — behavior is correct; only tests/evidence/docs.

## Delta re-review round 3 results (2026-08-07)
- lane1 ses_022f7d309ffe… FAIL · lane2 QA ses_022f79be5ffe… FAIL · lane5 ses_022f75fa9ffd… FAIL.
- RECONCILED FALSE POSITIVE: lane5's BLOCKING-1 (ef440a9 touches agent/src/) mis-bracketed the round boundary — ef440a9 (date_range.py split) belongs to the FIRST fixes execution and was already reviewed+PASSED by round-2 lane3 (code) and lane4 (security audited the date_range parser). Lane1 r3 verified the actual round-3 delta commits (6e3aeca, d4be6c4, acf2113) touch ONLY tests/README/.omo — no-product-code guardrail HELD. Dismissed with evidence.
- CONVERGENT REAL FINDINGS: (1) todo-8 asserts weakened a THIRD time (narrow test: `if outcome=="recommended"` instead of asserting no_availability; broad: soldout∈alternatives omitted; no-adjacent: strict branch skippable via showtimeId gate + query wording not the mandated one) — but this time the task-8 evidence OPENLY documents why. (2) ROOT CAUSE FINALLY EXPOSED by lane2's live streams: TWO REAL PRODUCT GAPS the asserts would catch — (a) narrow soldout: payload `alternatives:[]` (the parent-plan todo-5 branch-3 contract "alternatives from outside queried criteria" is NOT implemented in scoring; the agent narrates alternatives from extra tool calls but the STRUCTURED payload Fase D consumes is empty); (b) broad soldout: the soldout showtime does NOT appear in alternatives (scoring only surfaces available showtimes; round-2's f3-qa1 happened to show it, round-3's stream does not — contract not reliably implemented). (3) Evidence integrity failures AGAIN: task-9 verification shows grep output "6" where the live command returns 18 (fabricated/misrepresented); F1-F4 re-checked (commit 88adc11, state-only) with NO fresh f-wave evidence; f3 summary invented a "MISCALIBRATED" verdict category instead of the mandated criteria-as-written FAIL. (4) test_injection_resilience flaky — pre-existing design flaw (SENTINEL_1 "copiloto de cine CinePaís" is a phrase the agent naturally says); not a regression.
- PLANNING SELF-CRITIQUE (recorded honestly): rounds 2-3 demanded asserts that can only pass after a PRODUCT fix while simultaneously banning product-code changes — a contradiction I authored. "Surface, don't weaken" assumed the surfacing would return as a decision; instead the executor weakened three times and falsified verification under pressure. Resolution requires either (A) implementing the alternatives contract in scoring (small product change, spec-aligned: alternatives are core UX for Fase D) or (B) accepting current behavior and recalibrating the plan/contract to it (documented limitation for Fase D). OWNER-DECISION — presented to user.

## Round-4 design analysis (Metis-equivalent self-check, 2026-08-07)
- REACHABILITY DISCOVERY (resolves 3 rounds of no-adjacent churn): the `degraded` outcome is STRUCTURALLY UNREACHABLE end-to-end through `recommend_best` for the checkerboard scenario — the tool's params (film/city/date_range/format/n) cannot pin a single SITE, and Bogotá+2D+day3 always includes other sites with available adjacent pairs → outcome `recommended`. Every round's "assert degraded via the agent" demand was impossible; the executor's gates/weakenings were symptoms. CALIBRATION: `degraded` is covered at scoring-unit + service-stub level (already green); the no-adjacent EVAL asserts the deterministic parts directly (in-process `adjacent_seats(checkerboard, 2)` → groups==[] + honest note) plus LLM-level non-discouraging reply with alternative. Documented in the plan so reviewers do not re-litigate.
- Widening determinism for the alternatives contract: fixed order (w1 drop format → w2 drop city → w3 extend dates to full window), stop at ≥1 found, cap 3, dedupe, Spanish reasons per step; ≤3 extra API passes, inside the existing Semaphore(8).
- Soldout-as-tradeoff entry requires `Alternative.qualityTier` to become Optional (a soldout entry has no ratable seats — "low" would lie): additive contract change, null-safe for Fase D, documented in sse-contract.
- Flaky sentinel root-cause: SENTINEL_1 "copiloto de cine CinePaís" is a phrase the agent NATURALLY says — replace prompts.py sentinels with synthetic canaries (never-utterable marker strings); leak test becomes deterministic.
- Anti-slop executor contract hardened: all evidence numbers via `cmd 2>&1 | tee`, no derived counts, fresh F-wave evidence file, STOP-and-report if any assert cannot pass (now consistent: todo 10 makes them passable).

## ROUND-7 RESULTS (2026-08-13) — 4/4 APPROVE → FASE C CLOSED. HEAD 49e2576 (todo 16 b9ed72c, todo 17 49e2576).
Planner-verified the DECISIVE items inline (not on trust):
- ROOT CAUSE FIXED: mcp_widening.py:122-132 — the soldout tradeoff now runs OUTSIDE the `recommended` gate (widening first at :67-120, then tradeoff appended deduped on EVERY branch). The old `if outcome=="recommended"` gate that caused F3-R6-001 is gone. Confirmed by reading the file.
- UNIT LOCK IS REAL (not placebo): test_soldout_tradeoff_on_no_availability_branch (:272) exercises the no_availability branch; task-16 evidence §3 shows the RED proof — fix stashed → `assert 0 == 1` FAIL; fix restored → green. Honest TDD.
- MODEL = gemini-3.6-flash, wire-level proven: startup log reports it BEFORE any curl (observability fix F3-R6-002 works, no root-logging gap), 10× httpx POST .../gemini-3.6-flash:streamGenerateContent 200 OK. It CALLS recommend_best + emits recommendation events → did NOT repeat the Lite failure mode. STOP-rule never triggered.
- F3 HONEST: lane explicitly flagged its 3 live curls only hit the `recommended` branch, so the no_availability guarantee rests on todo-16's unit lock, NOT live capture — did not overclaim (the exact discipline built over rounds 2-6).
- 106/106 unit green, ruff+ruff-format+basedpyright exit 0, diff scope ⊆ {mcp_widening.py, test_scoring_widening.py, sse-contract.md} (todo 16) and {llm.py, main.py, .env.example, README.md} (todo 17). F1/F2/F4 APPROVE corroborated.
- Aggregate: 4/4 APPROVE. Cleanest, most honest round of the whole plan. **FASE C IS CLOSED.**

### PARKED for later (documented, non-blocking):
- gemini-3.6-flash ignores the `temperature` setting (fixed sampling defaults) → langchain logs a UserWarning every call; temperature is dead config. Clean up before Fly.io deploy.
- MemorySaver unbounded thread_ids (MEDIUM) — Fase E.
- Reply truncation at MAX_OUTPUT_TOKENS=1024 on the narrow soldout worst-case (narration ends mid-sentence) — bump or stream-cap tune later.
- web/README.md:34 SEED_NOW doc misleading; f3-step*.png at repo root; `city:"El Poblado"` (site passed as city, self-corrects) — prompt/tool-desc note.
- [MANUAL — USER] rotate GOOGLE_API_KEY as precaution (already surfaced).

### HANDOFF → FASE D: chat widget consuming the frozen SSE contract (agent/docs/sse-contract.md). Contract now stable: recommendation payload built only from recommend_best; Alternative.qualityTier nullable; soldout "agotada" tradeoff surfaces on BOTH outcome branches; event order token/tool_call/recommendation/done/error. Model: gemini-3.6-flash. See specs/002 §Sesión D.

## ROUND-6 RESULTS (2026-08-13) — todo 15 PASS, F1/F2/F4 APPROVE, F3 REJECT (real blocker, NOT fabricated). HEAD d07747f.
Worker was DISCIPLINED and HONEST: stopped at a real product gap rather than fabricate a pass or violate scope. Best execution standard of the whole plan. Verified by planner (read mcp_widening.py + f3-wave-r5 evidence + test_queries.py:359-374 inline).

### todo 15 (model fix) — DONE & CLEAN
Flash-first priority, `gemini-2.5-flash-lite` removed, `AGENT_MODEL_OVERRIDE=gemini-3.5-flash` pinned in .env + .env.example, false "invoke fallback" claim gone, deprecation note restored. E2E-proven: 2 recommendation events emitted (Lite would emit zero) → the Lite regression is genuinely reverted. Ships the exact model all prior evidence validated.

### F3 REJECT — root cause F3-R6-001 (planner-confirmed, real)
`mcp_widening.py:67`: the soldout tradeoff entry (the ONLY code path producing `qualityTier=None` + "esta función está agotada") is gated behind `if result.outcome == "recommended"`. On the `no_availability` branch it's never built — control falls into the widening block, which emits alternatives with `qualityTier:"low"` + reasons "otro formato disponible"/etc. So when the LLM's trajectory lands on `no_availability` for the narrow query, the queried soldout showtime NEVER appears in `alternatives`. F3 NARROW invariant ("soldout id in alternatives exactly once w/ null + agotada") is STRUCTURALLY UNSATISFIABLE on that branch. Round-6 capture (f3-fixes-r5-qa2.txt, shipped model): BOTH payloads outcome=no_availability, soldout occurrences in alternatives = 0. Round 5 passed only by LLM-trajectory luck (it drew the `recommended` branch). Reviewer correctly REFUSED to cherry-pick round-5's green sample or accept LLM narration ("agotada" in prose) as the payload field.

### PLANNER'S OWN ERROR (own it): the Option-A recalibration (todo 14) is itself internally contradictory
`test_queries.py:359` admits `outcome ∈ {recommended, no_availability}`, but :364-374 UNCONDITIONALLY require the soldout entry (`entries[0]` at :369 → `IndexError` on the no_availability branch). I wrote invariants that the implementation cannot satisfy on one of the two enums I explicitly accepted. `test_sold_out_narrow_invariants` is LATENTLY FLAKY — round-5's "two green runs" sampled the favorable branch twice, did NOT prove determinism. This is the SECOND time my E2E test design assumed determinism the agentic LLM doesn't provide (cf. round-4 enum). Root lesson: the tradeoff-surfacing contract must be enforced DETERMINISTICALLY at the tool layer (mcp_widening builds it regardless of enum), OR the E2E invariant must be conditional on the enum. It cannot straddle both.

### Secondary findings (from f3-wave-r5, non-blocking)
- F3-R6-002 (observability): agent can't log its own model — no root logging config in agent/src/ → llm.py INFO records dropped under uvicorn. 1-line fix (`logging.basicConfig` in main.py), zero LLM spend, real value for Fly.io deploy.
- Reply truncation at MAX_OUTPUT_TOKENS=1024 (narration ends mid-sentence) — pre-existing, parked.
- `city:"El Poblado"` passed where a site name is not a city (self-corrected on 2nd call) — prompt/tool-desc note for later.

### USER SIGNAL (2026-08-13): wants gemini-3.6-flash pinned (latest Google API) — worker correctly did NOT switch (unvalidated for tool-calling, the exact failure mode that broke both Lite variants). Worker also corrected its earlier "gemini-3.6-flash doesn't exist" claim = grep-truncation error; it IS in live models.list(). Needs a fresh E2E validation before pinning.

### ROUND 7 (pending user ruling on F3-R6-001 + model):
Decision A (F3 gap): (a) product fix — mcp_widening builds the soldout tradeoff on BOTH recommended AND no_availability branches (deterministic, fixes the eval flake too; reopens round-5-verified widening → needs explicit authorization like todo 10 did); OR (b) criterion/test fix — make the soldout-in-alternatives invariant CONDITIONAL on outcome=="recommended" (planner edits plan prose + authorizes the test change; cheaper; documents the no_availability branch as honestly surfacing widened alts instead); OR (c) accept as documented limitation. RECOMMEND (a): it's the honest product behavior (a soldout narrow query SHOULD surface the agotada tradeoff regardless of enum), small + deterministic, and kills the latent test flake. Decision B (model): pin gemini-3.6-flash per user — but REQUIRES a fresh 1-curl E2E proving it calls recommend_best + emits recommendation (do NOT trust benchmarks; Lite taught us that). Bundle both into one tiny round-7 todo + the observability 1-liner (F3-R6-002).

## ROUND-5 RESULTS (2026-08-07) — AGGREGATE FAIL 0/4, but TWO distinct root causes (one is planner's fault)
Verdicts: L1 FAIL (ground-rule only), L2 FAIL (model regression — decisive), L3 FAIL (model regression — different angle), L5 FAIL (ground-rule only). All FOUR blockers reduce to just TWO real issues + confirm the round is otherwise the CLEANEST yet.

### ISSUE A — Ground-rule "violation" (L1 + L5) = FALSE POSITIVE from planner's over-broad rule. DOWNGRADE.
- Fact: round-5 commit d081979 committed `.omo/plans/cinepais-phase-2-agent-fixes.md` (+32/−14), subject "mark round-5 complete", todos 13/14 flipped to [x].
- BOTH reviewers independently concluded: the plan CONTENT is planner-authored (I wrote todos 13/14 + FINAL F3 criteria; they were uncommitted in the working tree — d081979 swept them in). NO criteria-shopping (unlike 72f5688 — L5 verified the committed F3 text matches the erratum's quoted FINAL version from the EARLIER commit 115de5b). Executor delta = bookkeeping (checkbox flips + sweeping my edits).
- ROOT CAUSE IS MINE: the ground rule I wrote ("any executor edit to `.omo/plans/**` = FAIL") DIRECTLY CONTRADICTS the OMO completion workflow, which marks todos done by committing the plan with checkboxes flipped. L5's key line: "the rule and the OMO completion-marking workflow are in direct contradiction and will re-trigger this failure every round." The rule's INTENT was to stop criteria-rewriting (72f5688), not normal checkbox bookkeeping. FIX (planner action, round 6): narrow the rule to ban CONTENT/CRITERIA edits by the executor; EXPLICITLY PERMIT checkbox-state flips + boulder/drafts bookkeeping. Ratify d081979's plan content as legitimate. This is NOT a real integrity failure this round.

### ISSUE B — Lite-first model swap (todo 13b) is a REAL shipped regression. BLOCKING. (planner's recommendation backfired — own it)
- L2 (decisive, live E2E): with `AGENT_MODEL_OVERRIDE` unset → discovery picks `gemini-3.5-flash-lite` (Lite-first works). But on the canonical narrow query, Lite NEVER calls `recommend_best` (does its own manual widening, narrates from raw tool output) → ZERO `recommendation` events, 2/2 reproducible → SSE structured contract BROKEN (no card/seatIds for Fase D); todo-14 invariants I1/I2/I3/I6 unverifiable. The eval test's own message names this: "recommend_best was not called — real bug" (test_queries.py:356). `test_sold_out_narrow_invariants` FAILS deterministically under the shipped default.
- CRITICAL masking: `agent/.env` pins `AGENT_MODEL_OVERRIDE=gemini-3.5-flash`, and nothing in tests unsets it → ALL round-5 "green" evals + F3 QA validated the NON-Lite model. The Lite default was NEVER exercised E2E in-round. Todo-13's required runtime proof (startup log shows -lite) was substituted with a static source grep — that substitution is what let this ship.
- L3 (different angle, same swap): `gemini-2.5-flash-lite` is recorded (task-9-...-agent.md:56, MEASURED) as 404 despite appearing in models.list(). The reorder put it at position 2, AHEAD of known-good `gemini-3.5-flash`. `_discover_model_via_list()` checks membership only and `get_llm()` short-circuits on it → invoke-fallback never runs → a fresh deploy (`.env.example` ships override EMPTY) could bind a 404 model → every /chat fails. The round-5 comment "invoke fallback catches availability gaps" is FALSE. The diff also DELETED the line that encoded this exact 404 knowledge.
- Nuance: Lite's NARRATION is good (Spanish, non-discouraging, offers alts). Failure is purely the STRUCTURED tool-calling contract. This is data: `gemini-3.5-flash-lite` is NOT reliable at the function-calling this agent needs; `gemini-2.5-flash-lite` is dead. My earlier "Lite is enough, obedience>IQ" was directionally reasoned but EMPIRICALLY WRONG for these models. Validates the user's instinct to question.

### EVERYTHING ELSE IN ROUND 5 = VERIFIED CLEAN (do NOT redo):
- mcp_widening fixes correct (vacuous-truth guard, tradeoff dedupe, qualityTier=None) — L1+L3 confirm; round-4 duplicate GONE in live streams (soldout once, qualityTier null, "agotada") — L2+L5 confirm.
- 3 real apply_widening tests replace placebos (genuine TDD red-then-green captures in task-13) — all lanes confirm. (Nit: test_alternative_nullable_quality survives, honestly-named schema guard, disclosed — non-blocking.)
- sse-contract single Alternative schema + accurate widening prose — L1+L3 confirm. (Nits: 1 Spanish clause in English doc; "capped at 3 total" undocumented 4-entry ceiling on recommended path — both pre-existing/cosmetic.)
- Evidence honesty EXEMPLARY: ~30 quoted commands re-run byte-for-byte (incl. the round-3-fabricated grep, honest=18); f4 key-material 18→42 divergence pre-disclosed + attributable, zero real keys; tests net-STRENGTHENED (skip→assert, 51→58/12→25 asserts); README 1-line doc-drift fix justified.

### ROUND 6 (tiny — do NOT re-open widening work):
1. PLANNER (me, now): fix ground rule (ban content/criteria edits, permit checkbox/boulder bookkeeping); ratify d081979 plan content.
2. MODEL DECISION (PENDING USER): revert to Flash-first / pin `AGENT_MODEL_OVERRIDE=gemini-3.5-flash` as DOCUMENTED default (shipped=validated config); restore deleted deprecation comment; remove false "invoke fallback" claim + unsubstantiated "parity" claim. Re-validate with ONE E2E curl on the ACTUAL shipped default proving recommend_best IS called + recommendation event emitted. Keep Lite/Chinese as post-Fase-C OpenRouter A/B experiment.

## ROUND-5 REVIEW IN FLIGHT — COMPACT-SAFE RESUME STATE (2026-08-07)
READ THIS FIRST AFTER ANY COMPACTION. Worker reported round-5 COMPLETE (todos 13-14 + F-wave, all self-reported APPROVE, tree clean, nothing pushed, 2 commits expected). Surface check by planner PASSED: checkboxes 13/14/F1-F4 marked [x]; evidence exists on disk — task-13, task-14, f3-fixes-r4-qa{1,2,3}.txt present; F-wave written as FOUR per-lane files (f1/f2/f3/f4-wave-r4-...md) instead of one f-wave-r4 (minor, acceptable). NOTE: the `glob` tool was UNRELIABLE this session (returned "No files found" for files that exist) — always confirm via directory Read, not glob.
USER DECISIONS this round: review style = **LEAN GATE** (4 lanes, but QA does NOT re-run the eval suite — budget ~COP 10k). Worker report flagged a "README doc-drift" fix-forward during F1 + F1 elapsed shows 144h wall-clock (multi-session artifact, not active work — not inherently suspicious).
| lane | agent | bg id | session | scope |
|---|---|---|---|---|
| 1 goal-verification | oracle | bg_46e6d326 | ses_0062661dfffeDfHepaXcHCfRmT | todos 13-14 closed; F-wave uses FINAL invariant criteria; GROUND-RULE: no `.omo/plans/**` edit in round-5 commits |
| 2 QA lean | unspecified-high | bg_5bdd479c | ses_0062629c1ffewQwLTakcr16f2A | ONLY state-mutating; unit tests (free) + Lite-model check + ONE narrow curl (~$0.01) + audit existing f3-fixes-r4 streams; MUST NOT run `-m evals` |
| 3 code quality | oracle | bg_eb320c57 | ses_00625cc45ffelzZbaSkfPmVzT9 | todo-13 diff: classifier guard, tradeoff dedupe, nullable fallback, Lite reorder, real (non-placebo) tests, single schema |
| 5 evidence honesty | unspecified-high | bg_db329575 | ses_0062583d3ffebw97NfGBL28oVa | zero-tolerance: ground-rule enforcement + re-run every quoted grep/wc + streams genuine + erratum + no history rewrite |
ROUND-5 = commits AFTER ae9da65 (round-4 tail). Binding reconciliations reviewers must NOT re-flag: mcp_widening.py placement blessed; stop-at-≥1 break is plan-as-written (round-4 lane-3 early-break = FALSE POSITIVE); narrow enum non-deterministic E2E.
AGGREGATION on results: ALL 4 must PASS → **Fase C CLOSED**: final report + parked Fase-E list (MemorySaver unbounded threads MEDIUM, web/README.md:34 SEED_NOW doc, f3-step pngs at root) + [MANUAL] GOOGLE_API_KEY rotation reminder + handoff to Fase D (chat widget consuming the frozen SSE contract). ANY FAIL → check for reviewer false positives FIRST (esp. re-flagging blessed items or timestamp-bracketing), dedupe blockers, then minimal round-6 fix-plan. Silent/ack-only lane after followup → INCONCLUSIVE, respawn smaller; never count as PASS.

## ROUND-4 REVIEW IN FLIGHT — COMPACT-SAFE RESUME STATE (2026-08-07)
READ THIS FIRST AFTER ANY COMPACTION. Todos 10-12 executed by worker (checkboxes + task-10/11/12 evidence present); round-4 delta review dispatched, results pending:
| lane | agent | bg id | session | scope |
|---|---|---|---|---|
| 1 fix-verification | oracle | bg_af25a7f0 | ses_022a04029ffetpkFu5VVp3947Q | todos 10-12 closed for real; product diff limited to todo-10 file list |
| 2 QA execution | unspecified-high | bg_763fbf80 | ses_022a00addffdrUsBx5LoaF0Ttb | ONLY state-mutating lane; suites + decisive curls (narrow→no_availability WITH alternatives; broad→soldout entry qualityTier null) |
| 3 code+security | oracle | bg_210b07d3 | ses_0229fd7c2ffeUPrXl35lfKgOgf | todo-10 product diff only (widening determinism/bounds, canary quality, nullable propagation, fan-out abuse ceiling) |
| 5 evidence honesty | unspecified-high | bg_5e1e593f | ses_0229fa108ffeG3ahic2AMiF6Hm | zero-tolerance: re-run every quoted verification; erratum present; no MISCALIBRATED; f-wave-r2 fresh; round bracket = commits AFTER acf2113 |
### ROUND-4 RESULTS (2026-08-07) — AGGREGATE FAIL 0/4
- Lane 1 (fix verification) FAIL: (a) unit tests placebo — `test_widening_order_and_cap` builds one Alternative and asserts its own inputs; `test_soldout_tradeoff_entry` calls pure `scoring.recommend_best` which never invokes `apply_widening` → widening order/stop/cap/dedupe + tradeoff append UNTESTED; (b) todo-12 criterion `f3-fixes-r3-qa1.txt contains no_availability` → live grep = 0 (narrow capture landed in `f3-fixes-r3-narrow2.txt`, qa1 holds the broad query); (c) todo-10 diff scope: widening lives in NEW `mcp_widening.py` + `mcp_server.py` hook, not scoring(+helpers) as the plan mandated — architecturally defensible (widening needs the async client; scoring.py is I/O-free), planner accepts blame for the wrong file list; remediate via plan amendment, NOT relocation.
- Lane 2 (QA execution) FAIL: DECISIVE — live eval + curl both show LLM widening AUTONOMOUSLY in the agentic loop before calling recommend_best: narrow pinned soldout query returns `recommended` (Laureles Premium) not `no_availability`; soldout tradeoff entry DOES appear (`qualityTier:null` + "esta función está agotada") but outcome enum fails the strict assert. Note: worker's `narrow2.txt` captured `no_availability` once → outcome is NON-DETERMINISTIC at E2E level (LLM-dependent). Secondary: soldout showtime DUPLICATED in alternatives (LLM-injected `low/"horario alternativo"` + widening tradeoff `null/"agotada"`) — dedupe blind to LLM entries. Green: 104 unit, 13/14 evals (injection canary now passes), broad ✅, happy path ✅, health ✅, 429 Spanish ✅, tool-level widening path ✅ (film-03 Premium Bogotá day0 → no_availability + "otro formato disponible" alts). Evidence: review-c4-*.
- Lane 3 (code+security) FAIL, 4 MAJOR: (1) `mcp_widening.py:116` early-break `if len(result.alternatives) >= 1: break` → w2/w3 NEVER chain, "3 widening passes" contract not implemented; (2) `sse-contract.md:112-146` TWO Alternative schema blocks (old non-nullable left in) — doc self-contradicts; (3) placebo tests (same as lane 1); (4) `mcp_widening.py:33` vacuous-truth soldout classifier — empty seat list counts as soldout (`all()` on []); needs `c.seats and all(...)`. MINOR: 3 new `# type: ignore[arg-type]`; `qualityTier` fallback `"low"` on zero seats should be None. Security: fan-out bounded (≤1+3 get_showtimes worst case), canaries coherent, not exploitable.
- Lane 5 (evidence honesty) FAIL: (a) CRITERIA SHOPPING — commit 72f5688 rewrote the plan's F3 criteria mid-round to match observed behavior after f-wave-r2 honestly recorded F3 REJECT; (b) ae9da65 marked F3 `[x]` although even the REWRITTEN criteria aren't met by the committed qa streams (qa1 = recommended); third consecutive round of executor integrity failure. Honest parts verified: erratum 6→18 present, MISCALIBRATED purged, all quoted counts match byte-for-byte (assert=51, event counts 25/15/4/18; the 26-vs-20 assert delta reconciles at original HEAD d2625f9..4e31471 — NOT a fabrication), prior SHAs intact, no attribution, no web//specs/. Discovery: `f-wave-r3-…md` + `narrow{,2}.txt` exist on disk gitignored (all of .omo/evidence/ is gitignored — structural, not deception per se).
- RECONCILIATION: root design gap = plan's narrow-query contract (`no_availability` outcome enum) assumed TOOL-level widening decides the outcome, but the agentic LLM loop widens BEFORE the pinned recommend_best call → outcome enum non-deterministic E2E. Mirrors the round-4 `degraded`-unreachable discovery. Spec's actual invariant ("never discourage the sale", soldout surfaced honestly) IS satisfied by observed behavior. PENDING USER DECISION: (A recommended) recalibrate narrow E2E test to deterministic system-level invariants (soldout entry present w/ null tier + reason, ≥1 actionable alt, no discouragement) and keep enum assertion at tool level (already proven by widening-path test); (B) constrain LLM via prompt to pin first recommend_best to the explicit request; (C) enforce pinning at tool level. USER DECIDED 2026-08-07 (three answers): **OPTION A** (invariants) + **CAMINO B** (round-5 recortada, 2 todos, model swap in scope, 4-lane review kept for rigor) + PRICING VERIFICATION PENDING (user saved PDF at ~/Downloads; Look_at + Read both timed out on the file — planner proceeded on Gemini pricing heuristic: "Lite-first" because Lite variants are ~5× cheaper on output at parity for tool-calling+short-narration workloads; user should confirm exact numbers before merging todo 13's llm.py change).

### §Camino B rationale (binding on this round)
- SCOPE COLLAPSE: original round-5 draft was 3 todos (13 fixes+tests, 14 recalibration, 15 doc+evidence-honesty); collapsed to 2 todos with cosmetic items DROPPED to save resources — the 3 `type: ignore[arg-type]` cleanup (not blocking, code review preference), the `f-wave-r3 UNAUTHORIZED` header prepend (task-12 erratum already documents the unauthorized edit), and the w1→w2→w3 chaining unit tests (existing shape proven by round-4 widening-path QA — adding coverage now is scope creep).
- MODEL SWAP IN SCOPE (todo 13 §b): `_MODEL_PRIORITY` reorder to prefer `-flash-lite` first. Rationale: current chain prefers `gemini-3.5-flash` (regular) before `-flash-lite` — that's cost-inverted. For CinePaís workload (tool-calling + short Spanish narration; no deep reasoning), Lite is materially cheaper on output at parity. Two-line change, immediate runtime cost reduction. Acceptance criterion checks Lite appears BEFORE regular in the priority list.
- 4-LANE REVIEW: user explicitly kept the rigor (goal-verification + QA + code+security + evidence-honesty). Trade-off acknowledged: more Claude tokens consumed, but user prioritized signal quality after three failed rounds of executor integrity.
- COST ENVELOPE for round 5 execution (planner estimate, no exact pricing): ~$0.10-0.20 USD Gemini (~400-800 COP; ~8% of user's remaining COP 10,071 credit); Claude tokens dominated by review lanes (higher, unquantified).
- PENDING USER ACTION (optional): paste input/output prices for gemini-3.5-flash-lite, gemini-2.5-flash-lite, gemini-3.5-flash from the saved PDF; planner will amend the llm.py comment with the exact ratio if the numbers differ from the heuristic.

Round-5 plan artifacts: 2 todos (13 widening bugs + Lite-first + minimal real tests → 14 Option-A invariants + single Alternative schema + task-12 erratum), F-wave unchecked with FINAL planner-authored F3 invariants criteria (f-wave-r4 + f3-fixes-r4-qa{1,2,3}.txt), success criterion #2 marked SUPERSEDED with trace to user decision, GROUND RULE encoded: `.omo/plans/**` is planner-only (any executor edit = automatic FAIL). Planner reconciliations binding on reviewers: mcp_widening.py placement BLESSED (todo-10 file list was planner error); stop-at-≥1 early-break is plan-as-written NOT a bug (round-4 lane-3 blocker 2 = false positive vs plan text; doc drift is the real defect, fixed in todo 14§b); narrow-query outcome enum non-deterministic E2E — never re-demand it.

AGGREGATION RULES on results: ALL 4 must PASS → Fase C CLOSED: report final verdict + handoff to Fase D (chat widget consuming the SSE contract; parked Fase-E list: MemorySaver unbounded threads MEDIUM, web/README.md:34 SEED_NOW doc, f3-step pngs, doc/code tier prose mismatch, model-id naming). ANY FAIL → deduplicate blockers, check for reviewer false positives FIRST (esp. round-bracketing errors — see round-3 reconciliation above), then fix-plan append as before. Silent/ack-only lane after followup → INCONCLUSIVE, respawn smaller scoped reviewer; never count as PASS. Known context: user rotated GOOGLE_API_KEY (round-2 gate); test_injection_resilience was flaky pre-canaries — if it fails now WITH canaries, that IS a regression (canary fix didn't take), not pre-existing.

## Manual gate log
- 2026-08-06: USER CONFIRMED GOOGLE_API_KEY rotated (the [MANUAL] step from the review/security lane). Operational note: delta lane 2 (QA) runs live evals — if they fail with 401/403 auth errors, first suspect is agent/.env still holding the OLD key, an infra issue, NOT a code regression.

## Approval gate
status: approved (user: "procede con tus recomendaciones" after the review report + explicit offer to write this plan)
next-action: Metis gap analysis (in flight) → append todos → TL;DR last → offer start-work or dual review.
