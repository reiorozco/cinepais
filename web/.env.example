# Do not fill by hand — run `vercel env pull .env.local --yes` (see Todo 5 / README).
# Neon vars are injected by the Vercel Marketplace integration and pulled via vercel env pull.

DATABASE_URL="postgresql://..."
DATABASE_URL_UNPOOLED="postgresql://..."
SEED=20260801
SEED_NOW=2026-08-01T00:00:00-05:00

# CinePaís agent base URL (Fase E repoints to Fly.io production)
NEXT_PUBLIC_AGENT_URL="http://localhost:8000"
